final class {{classname}} implements JsonSerializable
{
    public const DISCRIMINATOR = {{#discriminator}}'{{discriminatorName}}'{{/discriminator}}{{^discriminator}}null{{/discriminator}};
    /**
     * The original name of the model.
     */
    private static string $openAPIModelName = '{{name}}';

    private static array $attributeMap = [
        {{#vars}}'{{name}}' => '{{baseName}}'{{^-last}},
        {{/-last}}{{/vars}}
    ];

    public function __construct(
    {{#vars}}
        {{#isContainer}}
        private readonly ?array ${{name}} = [],
        {{/isContainer}}
        {{^isContainer}}
        private readonly ?{{{dataType}}} ${{name}} = {{#defaultValue}}{{{defaultValue}}}{{/defaultValue}}{{^defaultValue}}null{{/defaultValue}},
        {{/isContainer}}
    {{/vars}}
    ) {
    }

    public function jsonSerialize(): array
    {
        return [
            {{#vars}}
            '{{name}}' => $this->{{name}},
            {{/vars}}
        ];
    }

    public function __toString(): string
    {
        return json_encode($this->jsonSerialize(), JSON_PRETTY_PRINT);
    }

    {{#vars}}
    public function {{{getter}}}(): {{#isContainer}}?array{{/isContainer}}{{^isContainer}}?{{{datatype}}}{{/isContainer}}
    {
        return $this->{{{name}}};
    }
    {{/vars}}
}
