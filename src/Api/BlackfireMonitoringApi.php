<?php

namespace Upsun\Api;

use DateTime;
use Exception;
use GuzzleHttp\Psr7\MultipartStream;
use InvalidArgumentException;
use Psr\Http\Client\ClientExceptionInterface;
use Psr\Http\Client\ClientInterface;
use Psr\Http\Message\RequestFactoryInterface;
use Psr\Http\Message\RequestInterface;
use Psr\Http\Message\StreamFactoryInterface;
use Upsun\Api\Serializer\ObjectSerializer;
use Upsun\Core\OAuthProvider;

/**
 * Low level BlackfireMonitoringApi (auto-generated)
 *
 * @license   Apache-2.0
 * @see       https://docs.upsun.com
 * @generated This file was generated by OpenAPI Generator. Do not edit manually.
 */
final class BlackfireMonitoringApi extends AbstractApi
{
    private readonly ApiHeaderSelector $headerSelector;

    private APIConfiguration $config;

    public function __construct(
        OAuthProvider $oauthProvider,
        ?ClientInterface $httpClient = null,
        ?RequestFactoryInterface $requestFactory = null,
        ?APIConfiguration $config = null,
        ?StreamFactoryInterface $streamFactory = null,
        ?ApiHeaderSelector $selector = null,
    ) {
        parent::__construct(
            $oauthProvider,
            $httpClient,
            $requestFactory,
            AbstractApi::BASE_PATH,
            $streamFactory
        );

        $this->config = $config ?? (new APIConfiguration())->setHost(AbstractApi::BASE_PATH);

        $this->headerSelector = $selector ?? new ApiHeaderSelector();
    }

    /**
     * Get server cache metrics
     *
     * Returns time-series PHP cache usage ratios (0.0-1.0) for OPcache memory, OPcache interned strings buffer (ISB),
     * APCu memory, realpath cache, and PCRE cache. Includes hit rate ratios (0.0-1.0) for APCu and OPcache. Each data
     * point contains `timestamp` and optional data fields. Fields are omitted for timestamps with no data - this
     * indicates no data was received. Filtered by agent ID and execution contexts (web, cli, etc.).
     *
     * @param  string $projectId (required)
     * @param  string $environmentId (required)
     * @param  int $from (required)
     * @param  int $to (required)
     * @param  int|null $grain (optional)
     * @param  string[]|null $contexts (optional)
     * @param  string|null $contextsMode (optional)
     * @param  string[]|null $applications (optional)
     * @param  string|null $applicationsMode (optional)
     * @param  string[]|null $instances (optional)
     * @param  string|null $instancesMode (optional)
     * @param  string|null $distributionCost (optional)
     *
     * @throws ApiException on non-2xx response or if the response body is not in the expected format
     * @throws ClientExceptionInterface
     * @see https://docs.upsun.com/api/#tag/Blackfire-Monitoring/operation/blackfire_php_server_caches
     */
    public function blackfirePhpServerCaches(
        string $projectId,
        string $environmentId,
        int $from,
        int $to,
        ?int $grain = null,
        ?array $contexts = null,
        ?string $contextsMode = null,
        ?array $applications = null,
        ?string $applicationsMode = null,
        ?array $instances = null,
        ?string $instancesMode = null,
        ?string $distributionCost = null
    ): mixed {
        return $this->blackfirePhpServerCachesWithHttpInfo(
            $projectId,
            $environmentId,
            $from,
            $to,
            $grain,
            $contexts,
            $contextsMode,
            $applications,
            $applicationsMode,
            $instances,
            $instancesMode,
            $distributionCost
        );
    }

    /**
     * Get server cache metrics with HTTP Info
     *
     * @param  string $projectId (required)
     * @param  string $environmentId (required)
     * @param  int $from (required)
     * @param  int $to (required)
     * @param  int|null $grain (optional)
     * @param  string[]|null $contexts (optional)
     * @param  string|null $contextsMode (optional)
     * @param  string[]|null $applications (optional)
     * @param  string|null $applicationsMode (optional)
     * @param  string[]|null $instances (optional)
     * @param  string|null $instancesMode (optional)
     * @param  string|null $distributionCost (optional)
     *
     * @throws ApiException on non-2xx response or if the response body is not in the expected format
     * @throws ClientExceptionInterface
    */
    private function blackfirePhpServerCachesWithHttpInfo(
        string $projectId,
        string $environmentId,
        int $from,
        int $to,
        ?int $grain = null,
        ?array $contexts = null,
        ?string $contextsMode = null,
        ?array $applications = null,
        ?string $applicationsMode = null,
        ?array $instances = null,
        ?string $instancesMode = null,
        ?string $distributionCost = null
    ): mixed {
        $request = $this->blackfirePhpServerCachesRequest(
            $projectId,
            $environmentId,
            $from,
            $to,
            $grain,
            $contexts,
            $contextsMode,
            $applications,
            $applicationsMode,
            $instances,
            $instancesMode,
            $distributionCost
        );

        try {
            $response = $this->sendAuthenticatedRequest(
                $request->getMethod(),
                (string) $request->getUri(),
                $request->getHeaders(),
                $request->getBody()
            );

            return $this->handleResponseWithDataType(
                '\Upsun\Model\BlackfirePhpServerCaches200Response',
                $request,
                $response
            );
        } catch (Exception $exception) {
            throw new ApiException(
                sprintf(
                    '[%d] Error connecting to the API (%s)',
                    $exception->getCode(),
                    '/projects/{projectId}/environments/{environmentId}/observability/server/caches'
                ),
                $request,
                $response ?? null,
                $exception
            );
        }
    }

    /**
     * Create request for operation 'blackfirePhpServerCaches'
     *
     * @param  string $projectId (required)
     * @param  string $environmentId (required)
     * @param  int $from (required)
     * @param  int $to (required)
     * @param  int|null $grain (optional)
     * @param  string[]|null $contexts (optional)
     * @param  string|null $contextsMode (optional)
     * @param  string[]|null $applications (optional)
     * @param  string|null $applicationsMode (optional)
     * @param  string[]|null $instances (optional)
     * @param  string|null $instancesMode (optional)
     * @param  string|null $distributionCost (optional)
     *
     * @throws InvalidArgumentException
     */
    private function blackfirePhpServerCachesRequest(
        string $projectId,
        string $environmentId,
        int $from,
        int $to,
        ?int $grain = null,
        ?array $contexts = null,
        ?string $contextsMode = null,
        ?array $applications = null,
        ?string $applicationsMode = null,
        ?array $instances = null,
        ?string $instancesMode = null,
        ?string $distributionCost = null
    ): RequestInterface {
        // verify the required parameter 'projectId' is set
        if (empty($projectId)) {
            throw new InvalidArgumentException(
                'Missing the required parameter $projectId
                when calling blackfirePhpServerCaches'
            );
        }

        if (!preg_match("/[a-z0-9]+/", $projectId)) {
            throw new InvalidArgumentException(
                "invalid value for \"projectId\" when calling BlackfireMonitoringApi.blackfirePhpServerCaches,
                must conform to the pattern /[a-z0-9]+/."
            );
        }

        // verify the required parameter 'environmentId' is set
        if (empty($environmentId)) {
            throw new InvalidArgumentException(
                'Missing the required parameter $environmentId
                when calling blackfirePhpServerCaches'
            );
        }

        if (!preg_match("/.+/", $environmentId)) {
            throw new InvalidArgumentException(
                "invalid value for \"environmentId\" when calling BlackfireMonitoringApi.blackfirePhpServerCaches,
                must conform to the pattern /.+/."
            );
        }

        // verify the required parameter 'from' is set
        if (empty($from)) {
            throw new InvalidArgumentException(
                'Missing the required parameter $from
                when calling blackfirePhpServerCaches'
            );
        }
        // verify the required parameter 'to' is set
        if (empty($to)) {
            throw new InvalidArgumentException(
                'Missing the required parameter $to
                when calling blackfirePhpServerCaches'
            );
        }

        $resourcePath = '/projects/{projectId}/environments/{environmentId}/observability/server/caches';
        $formParams = [];
        $queryParams = [];
        $headerParams = [];
        $httpBody = null;
        $multipart = false;

        // query params
        if ($from !== null) {
            if ('form' === 'form' && is_array($from)) {
                foreach ($from as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['from'] = $from instanceof DateTime
                    ? $from->format(DATE_ATOM)
                    : ($from);
            }
        }

        // query params
        if ($to !== null) {
            if ('form' === 'form' && is_array($to)) {
                foreach ($to as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['to'] = $to instanceof DateTime
                    ? $to->format(DATE_ATOM)
                    : ($to);
            }
        }

        // query params
        if ($grain !== null) {
            if ('form' === 'form' && is_array($grain)) {
                foreach ($grain as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['grain'] = $grain instanceof DateTime
                    ? $grain->format(DATE_ATOM)
                    : ($grain);
            }
        }

        // query params
        if ($contexts !== null) {
            if ('form' === 'form' && is_array($contexts)) {
                foreach ($contexts as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['contexts[]'] = $contexts instanceof DateTime
                    ? $contexts->format(DATE_ATOM)
                    : ($contexts);
            }
        }

        // query params
        if ($contextsMode !== null) {
            if ('form' === 'form' && is_array($contextsMode)) {
                foreach ($contextsMode as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['contexts_mode'] = $contextsMode instanceof DateTime
                    ? $contextsMode->format(DATE_ATOM)
                    : ($contextsMode);
            }
        }

        // query params
        if ($applications !== null) {
            if ('form' === 'form' && is_array($applications)) {
                foreach ($applications as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['applications[]'] = $applications instanceof DateTime
                    ? $applications->format(DATE_ATOM)
                    : ($applications);
            }
        }

        // query params
        if ($applicationsMode !== null) {
            if ('form' === 'form' && is_array($applicationsMode)) {
                foreach ($applicationsMode as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['applications_mode'] = $applicationsMode instanceof DateTime
                    ? $applicationsMode->format(DATE_ATOM)
                    : ($applicationsMode);
            }
        }

        // query params
        if ($instances !== null) {
            if ('form' === 'form' && is_array($instances)) {
                foreach ($instances as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['instances[]'] = $instances instanceof DateTime
                    ? $instances->format(DATE_ATOM)
                    : ($instances);
            }
        }

        // query params
        if ($instancesMode !== null) {
            if ('form' === 'form' && is_array($instancesMode)) {
                foreach ($instancesMode as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['instances_mode'] = $instancesMode instanceof DateTime
                    ? $instancesMode->format(DATE_ATOM)
                    : ($instancesMode);
            }
        }

        // query params
        if ($distributionCost !== null) {
            if ('form' === 'form' && is_array($distributionCost)) {
                foreach ($distributionCost as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['distribution_cost'] = $distributionCost instanceof DateTime
                    ? $distributionCost->format(DATE_ATOM)
                    : ($distributionCost);
            }
        }

        // path params

        if ($projectId !== null) {
            $resourcePath = str_replace(
                '{' . 'projectId' . '}',
                ObjectSerializer::toPathValue($projectId),
                $resourcePath
            );
        }
        // path params

        if ($environmentId !== null) {
            $resourcePath = str_replace(
                '{' . 'environmentId' . '}',
                ObjectSerializer::toPathValue($environmentId),
                $resourcePath
            );
        }

        $headers = $this->headerSelector->selectHeaders(
            ['application/json'],
            '',
            $multipart
        );

        // for model (json/xml)
        if ($formParams !== []) {
            if ($multipart) {
                $multipartContents = [];
                foreach ($formParams as $formParamName => $formParamValue) {
                    $formParamValueItems = is_array($formParamValue) ? $formParamValue : [$formParamValue];
                    foreach ($formParamValueItems as $formParamValueItem) {
                        $multipartContents[] = [
                            'name' => $formParamName,
                            'contents' => $formParamValueItem
                        ];
                    }
                }

                // for HTTP post (form)
                $httpBody = new MultipartStream($multipartContents);
            } elseif ($this->headerSelector->isJsonMime($headers['Content-Type'])) {
                $httpBody = json_encode($formParams);
            } else {
                // for HTTP post (form)
                $httpBody = ObjectSerializer::buildQuery($formParams);
            }
        }

        // this endpoint requires Bearer authentication (access token)
        if ($this->config->getAccessToken() !== null) {
            $headers['Authorization'] = 'Bearer ' . $this->config->getAccessToken();
        }

        $defaultHeaders = [];
        if ($this->config->getUserAgent()) {
            $defaultHeaders['User-Agent'] = $this->config->getUserAgent();
        }
        if ($this->config->getUpsunClientHeader()) {
            $defaultHeaders['X-Upsun-Client'] = $this->config->getUpsunClientHeader();
        }

        $headers = array_merge(
            $defaultHeaders,
            $headerParams,
            $headers
        );

        $operationHost = $this->config->getHost();

        $uri = $this->createUri($operationHost, $resourcePath, $queryParams);

        return $this->createRequest('GET', $uri, $headers, $httpBody);
    }

    /**
     * Get server monitoring overview
     *
     * Returns time-series server monitoring data for the specified metrics (wall time, memory usage, peak memory usage,
     * CPU load, stdout, requests per minute). Includes alert evaluations and APM quota usage. Data points contain
     * metric values aggregated at the requested granularity.
     *
     * @param  string $projectId (required)
     * @param  string $environmentId (required)
     * @param  int $from (required)
     * @param  int $to (required)
     * @param  string[] $keys (required)
     * @param  int|null $grain (optional)
     * @param  string[]|null $contexts (optional)
     * @param  string|null $contextsMode (optional)
     * @param  string[]|null $applications (optional)
     * @param  string|null $applicationsMode (optional)
     * @param  string[]|null $instances (optional)
     * @param  string|null $instancesMode (optional)
     * @param  string|null $distributionCost (optional)
     *
     * @throws ApiException on non-2xx response or if the response body is not in the expected format
     * @throws ClientExceptionInterface
     * @see https://docs.upsun.com/api/#tag/Blackfire-Monitoring/operation/blackfire_server_global
     */
    public function blackfireServerGlobal(
        string $projectId,
        string $environmentId,
        int $from,
        int $to,
        array $keys,
        ?int $grain = null,
        ?array $contexts = null,
        ?string $contextsMode = null,
        ?array $applications = null,
        ?string $applicationsMode = null,
        ?array $instances = null,
        ?string $instancesMode = null,
        ?string $distributionCost = null
    ): mixed {
        return $this->blackfireServerGlobalWithHttpInfo(
            $projectId,
            $environmentId,
            $from,
            $to,
            $keys,
            $grain,
            $contexts,
            $contextsMode,
            $applications,
            $applicationsMode,
            $instances,
            $instancesMode,
            $distributionCost
        );
    }

    /**
     * Get server monitoring overview with HTTP Info
     *
     * @param  string $projectId (required)
     * @param  string $environmentId (required)
     * @param  int $from (required)
     * @param  int $to (required)
     * @param  string[] $keys (required)
     * @param  int|null $grain (optional)
     * @param  string[]|null $contexts (optional)
     * @param  string|null $contextsMode (optional)
     * @param  string[]|null $applications (optional)
     * @param  string|null $applicationsMode (optional)
     * @param  string[]|null $instances (optional)
     * @param  string|null $instancesMode (optional)
     * @param  string|null $distributionCost (optional)
     *
     * @throws ApiException on non-2xx response or if the response body is not in the expected format
     * @throws ClientExceptionInterface
    */
    private function blackfireServerGlobalWithHttpInfo(
        string $projectId,
        string $environmentId,
        int $from,
        int $to,
        array $keys,
        ?int $grain = null,
        ?array $contexts = null,
        ?string $contextsMode = null,
        ?array $applications = null,
        ?string $applicationsMode = null,
        ?array $instances = null,
        ?string $instancesMode = null,
        ?string $distributionCost = null
    ): mixed {
        $request = $this->blackfireServerGlobalRequest(
            $projectId,
            $environmentId,
            $from,
            $to,
            $keys,
            $grain,
            $contexts,
            $contextsMode,
            $applications,
            $applicationsMode,
            $instances,
            $instancesMode,
            $distributionCost
        );

        try {
            $response = $this->sendAuthenticatedRequest(
                $request->getMethod(),
                (string) $request->getUri(),
                $request->getHeaders(),
                $request->getBody()
            );

            return $this->handleResponseWithDataType(
                '\Upsun\Model\BlackfireServerGlobal200Response',
                $request,
                $response
            );
        } catch (Exception $exception) {
            throw new ApiException(
                sprintf(
                    '[%d] Error connecting to the API (%s)',
                    $exception->getCode(),
                    '/projects/{projectId}/environments/{environmentId}/observability/server'
                ),
                $request,
                $response ?? null,
                $exception
            );
        }
    }

    /**
     * Create request for operation 'blackfireServerGlobal'
     *
     * @param  string $projectId (required)
     * @param  string $environmentId (required)
     * @param  int $from (required)
     * @param  int $to (required)
     * @param  string[] $keys (required)
     * @param  int|null $grain (optional)
     * @param  string[]|null $contexts (optional)
     * @param  string|null $contextsMode (optional)
     * @param  string[]|null $applications (optional)
     * @param  string|null $applicationsMode (optional)
     * @param  string[]|null $instances (optional)
     * @param  string|null $instancesMode (optional)
     * @param  string|null $distributionCost (optional)
     *
     * @throws InvalidArgumentException
     */
    private function blackfireServerGlobalRequest(
        string $projectId,
        string $environmentId,
        int $from,
        int $to,
        array $keys,
        ?int $grain = null,
        ?array $contexts = null,
        ?string $contextsMode = null,
        ?array $applications = null,
        ?string $applicationsMode = null,
        ?array $instances = null,
        ?string $instancesMode = null,
        ?string $distributionCost = null
    ): RequestInterface {
        // verify the required parameter 'projectId' is set
        if (empty($projectId)) {
            throw new InvalidArgumentException(
                'Missing the required parameter $projectId
                when calling blackfireServerGlobal'
            );
        }

        if (!preg_match("/[a-z0-9]+/", $projectId)) {
            throw new InvalidArgumentException(
                "invalid value for \"projectId\" when calling BlackfireMonitoringApi.blackfireServerGlobal,
                must conform to the pattern /[a-z0-9]+/."
            );
        }

        // verify the required parameter 'environmentId' is set
        if (empty($environmentId)) {
            throw new InvalidArgumentException(
                'Missing the required parameter $environmentId
                when calling blackfireServerGlobal'
            );
        }

        if (!preg_match("/.+/", $environmentId)) {
            throw new InvalidArgumentException(
                "invalid value for \"environmentId\" when calling BlackfireMonitoringApi.blackfireServerGlobal,
                must conform to the pattern /.+/."
            );
        }

        // verify the required parameter 'from' is set
        if (empty($from)) {
            throw new InvalidArgumentException(
                'Missing the required parameter $from
                when calling blackfireServerGlobal'
            );
        }
        // verify the required parameter 'to' is set
        if (empty($to)) {
            throw new InvalidArgumentException(
                'Missing the required parameter $to
                when calling blackfireServerGlobal'
            );
        }
        // verify the required parameter 'keys' is set
        if (empty($keys)) {
            throw new InvalidArgumentException(
                'Missing the required parameter $keys
                when calling blackfireServerGlobal'
            );
        }

        $resourcePath = '/projects/{projectId}/environments/{environmentId}/observability/server';
        $formParams = [];
        $queryParams = [];
        $headerParams = [];
        $httpBody = null;
        $multipart = false;

        // query params
        if ($from !== null) {
            if ('form' === 'form' && is_array($from)) {
                foreach ($from as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['from'] = $from instanceof DateTime
                    ? $from->format(DATE_ATOM)
                    : ($from);
            }
        }

        // query params
        if ($to !== null) {
            if ('form' === 'form' && is_array($to)) {
                foreach ($to as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['to'] = $to instanceof DateTime
                    ? $to->format(DATE_ATOM)
                    : ($to);
            }
        }

        // query params
        if ($keys !== null) {
            if ('form' === 'form' && is_array($keys)) {
                foreach ($keys as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['keys[]'] = $keys instanceof DateTime
                    ? $keys->format(DATE_ATOM)
                    : ($keys);
            }
        }

        // query params
        if ($grain !== null) {
            if ('form' === 'form' && is_array($grain)) {
                foreach ($grain as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['grain'] = $grain instanceof DateTime
                    ? $grain->format(DATE_ATOM)
                    : ($grain);
            }
        }

        // query params
        if ($contexts !== null) {
            if ('form' === 'form' && is_array($contexts)) {
                foreach ($contexts as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['contexts[]'] = $contexts instanceof DateTime
                    ? $contexts->format(DATE_ATOM)
                    : ($contexts);
            }
        }

        // query params
        if ($contextsMode !== null) {
            if ('form' === 'form' && is_array($contextsMode)) {
                foreach ($contextsMode as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['contexts_mode'] = $contextsMode instanceof DateTime
                    ? $contextsMode->format(DATE_ATOM)
                    : ($contextsMode);
            }
        }

        // query params
        if ($applications !== null) {
            if ('form' === 'form' && is_array($applications)) {
                foreach ($applications as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['applications[]'] = $applications instanceof DateTime
                    ? $applications->format(DATE_ATOM)
                    : ($applications);
            }
        }

        // query params
        if ($applicationsMode !== null) {
            if ('form' === 'form' && is_array($applicationsMode)) {
                foreach ($applicationsMode as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['applications_mode'] = $applicationsMode instanceof DateTime
                    ? $applicationsMode->format(DATE_ATOM)
                    : ($applicationsMode);
            }
        }

        // query params
        if ($instances !== null) {
            if ('form' === 'form' && is_array($instances)) {
                foreach ($instances as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['instances[]'] = $instances instanceof DateTime
                    ? $instances->format(DATE_ATOM)
                    : ($instances);
            }
        }

        // query params
        if ($instancesMode !== null) {
            if ('form' === 'form' && is_array($instancesMode)) {
                foreach ($instancesMode as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['instances_mode'] = $instancesMode instanceof DateTime
                    ? $instancesMode->format(DATE_ATOM)
                    : ($instancesMode);
            }
        }

        // query params
        if ($distributionCost !== null) {
            if ('form' === 'form' && is_array($distributionCost)) {
                foreach ($distributionCost as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['distribution_cost'] = $distributionCost instanceof DateTime
                    ? $distributionCost->format(DATE_ATOM)
                    : ($distributionCost);
            }
        }

        // path params

        if ($projectId !== null) {
            $resourcePath = str_replace(
                '{' . 'projectId' . '}',
                ObjectSerializer::toPathValue($projectId),
                $resourcePath
            );
        }
        // path params

        if ($environmentId !== null) {
            $resourcePath = str_replace(
                '{' . 'environmentId' . '}',
                ObjectSerializer::toPathValue($environmentId),
                $resourcePath
            );
        }

        $headers = $this->headerSelector->selectHeaders(
            ['application/json'],
            '',
            $multipart
        );

        // for model (json/xml)
        if ($formParams !== []) {
            if ($multipart) {
                $multipartContents = [];
                foreach ($formParams as $formParamName => $formParamValue) {
                    $formParamValueItems = is_array($formParamValue) ? $formParamValue : [$formParamValue];
                    foreach ($formParamValueItems as $formParamValueItem) {
                        $multipartContents[] = [
                            'name' => $formParamName,
                            'contents' => $formParamValueItem
                        ];
                    }
                }

                // for HTTP post (form)
                $httpBody = new MultipartStream($multipartContents);
            } elseif ($this->headerSelector->isJsonMime($headers['Content-Type'])) {
                $httpBody = json_encode($formParams);
            } else {
                // for HTTP post (form)
                $httpBody = ObjectSerializer::buildQuery($formParams);
            }
        }

        // this endpoint requires Bearer authentication (access token)
        if ($this->config->getAccessToken() !== null) {
            $headers['Authorization'] = 'Bearer ' . $this->config->getAccessToken();
        }

        $defaultHeaders = [];
        if ($this->config->getUserAgent()) {
            $defaultHeaders['User-Agent'] = $this->config->getUserAgent();
        }
        if ($this->config->getUpsunClientHeader()) {
            $defaultHeaders['X-Upsun-Client'] = $this->config->getUpsunClientHeader();
        }

        $headers = array_merge(
            $defaultHeaders,
            $headerParams,
            $headers
        );

        $operationHost = $this->config->getHost();

        $uri = $this->createUri($operationHost, $resourcePath, $queryParams);

        return $this->createRequest('GET', $uri, $headers, $httpBody);
    }

    /**
     * Get top spans
     *
     * Returns the top spans aggregated across traces, sorted by impact or other metrics. Each span includes wall time,
     * P96, call count, percentage of traces containing the span, and impact score. Supports filtering by context,
     * transaction, host, framework, runtime, HTTP method/status, and service layer.
     *
     * @param  string $projectId (required)
     * @param  string $environmentId (required)
     * @param  int $from (required)
     * @param  int $to (required)
     * @param  int|null $grain (optional)
     * @param  string|null $sort (optional)
     * @param  string[]|null $contexts (optional)
     * @param  string|null $contextsMode (optional)
     * @param  string[]|null $applications (optional)
     * @param  string|null $applicationsMode (optional)
     * @param  string[]|null $instances (optional)
     * @param  string|null $instancesMode (optional)
     * @param  string[]|null $transactions (optional)
     * @param  string|null $transactionsMode (optional)
     * @param  string[]|null $wtSlots (optional)
     * @param  string|null $wtSlotsMode (optional)
     * @param  string[]|null $pmuSlots (optional)
     * @param  string|null $pmuSlotsMode (optional)
     * @param  string[]|null $httpStatusCodes (optional)
     * @param  string|null $httpStatusCodesMode (optional)
     * @param  string[]|null $httpHosts (optional)
     * @param  string|null $httpHostsMode (optional)
     * @param  string[]|null $hosts (optional)
     * @param  string|null $hostsMode (optional)
     * @param  string[]|null $frameworks (optional)
     * @param  string|null $frameworksMode (optional)
     * @param  string[]|null $languages (optional)
     * @param  string|null $languagesMode (optional)
     * @param  string[]|null $methods (optional)
     * @param  string|null $methodsMode (optional)
     * @param  string[]|null $runtimes (optional)
     * @param  string|null $runtimesMode (optional)
     * @param  string[]|null $oss (optional)
     * @param  string|null $ossMode (optional)
     * @param  string|null $distributionCost (optional)
     *
     * @throws ApiException on non-2xx response or if the response body is not in the expected format
     * @throws ClientExceptionInterface
     * @see https://docs.upsun.com/api/#tag/Blackfire-Monitoring/operation/blackfire_server_top_spans
     */
    public function blackfireServerTopSpans(
        string $projectId,
        string $environmentId,
        int $from,
        int $to,
        ?int $grain = null,
        ?string $sort = null,
        ?array $contexts = null,
        ?string $contextsMode = null,
        ?array $applications = null,
        ?string $applicationsMode = null,
        ?array $instances = null,
        ?string $instancesMode = null,
        ?array $transactions = null,
        ?string $transactionsMode = null,
        ?array $wtSlots = null,
        ?string $wtSlotsMode = null,
        ?array $pmuSlots = null,
        ?string $pmuSlotsMode = null,
        ?array $httpStatusCodes = null,
        ?string $httpStatusCodesMode = null,
        ?array $httpHosts = null,
        ?string $httpHostsMode = null,
        ?array $hosts = null,
        ?string $hostsMode = null,
        ?array $frameworks = null,
        ?string $frameworksMode = null,
        ?array $languages = null,
        ?string $languagesMode = null,
        ?array $methods = null,
        ?string $methodsMode = null,
        ?array $runtimes = null,
        ?string $runtimesMode = null,
        ?array $oss = null,
        ?string $ossMode = null,
        ?string $distributionCost = null
    ): mixed {
        return $this->blackfireServerTopSpansWithHttpInfo(
            $projectId,
            $environmentId,
            $from,
            $to,
            $grain,
            $sort,
            $contexts,
            $contextsMode,
            $applications,
            $applicationsMode,
            $instances,
            $instancesMode,
            $transactions,
            $transactionsMode,
            $wtSlots,
            $wtSlotsMode,
            $pmuSlots,
            $pmuSlotsMode,
            $httpStatusCodes,
            $httpStatusCodesMode,
            $httpHosts,
            $httpHostsMode,
            $hosts,
            $hostsMode,
            $frameworks,
            $frameworksMode,
            $languages,
            $languagesMode,
            $methods,
            $methodsMode,
            $runtimes,
            $runtimesMode,
            $oss,
            $ossMode,
            $distributionCost
        );
    }

    /**
     * Get top spans with HTTP Info
     *
     * @param  string $projectId (required)
     * @param  string $environmentId (required)
     * @param  int $from (required)
     * @param  int $to (required)
     * @param  int|null $grain (optional)
     * @param  string|null $sort (optional)
     * @param  string[]|null $contexts (optional)
     * @param  string|null $contextsMode (optional)
     * @param  string[]|null $applications (optional)
     * @param  string|null $applicationsMode (optional)
     * @param  string[]|null $instances (optional)
     * @param  string|null $instancesMode (optional)
     * @param  string[]|null $transactions (optional)
     * @param  string|null $transactionsMode (optional)
     * @param  string[]|null $wtSlots (optional)
     * @param  string|null $wtSlotsMode (optional)
     * @param  string[]|null $pmuSlots (optional)
     * @param  string|null $pmuSlotsMode (optional)
     * @param  string[]|null $httpStatusCodes (optional)
     * @param  string|null $httpStatusCodesMode (optional)
     * @param  string[]|null $httpHosts (optional)
     * @param  string|null $httpHostsMode (optional)
     * @param  string[]|null $hosts (optional)
     * @param  string|null $hostsMode (optional)
     * @param  string[]|null $frameworks (optional)
     * @param  string|null $frameworksMode (optional)
     * @param  string[]|null $languages (optional)
     * @param  string|null $languagesMode (optional)
     * @param  string[]|null $methods (optional)
     * @param  string|null $methodsMode (optional)
     * @param  string[]|null $runtimes (optional)
     * @param  string|null $runtimesMode (optional)
     * @param  string[]|null $oss (optional)
     * @param  string|null $ossMode (optional)
     * @param  string|null $distributionCost (optional)
     *
     * @throws ApiException on non-2xx response or if the response body is not in the expected format
     * @throws ClientExceptionInterface
    */
    private function blackfireServerTopSpansWithHttpInfo(
        string $projectId,
        string $environmentId,
        int $from,
        int $to,
        ?int $grain = null,
        ?string $sort = null,
        ?array $contexts = null,
        ?string $contextsMode = null,
        ?array $applications = null,
        ?string $applicationsMode = null,
        ?array $instances = null,
        ?string $instancesMode = null,
        ?array $transactions = null,
        ?string $transactionsMode = null,
        ?array $wtSlots = null,
        ?string $wtSlotsMode = null,
        ?array $pmuSlots = null,
        ?string $pmuSlotsMode = null,
        ?array $httpStatusCodes = null,
        ?string $httpStatusCodesMode = null,
        ?array $httpHosts = null,
        ?string $httpHostsMode = null,
        ?array $hosts = null,
        ?string $hostsMode = null,
        ?array $frameworks = null,
        ?string $frameworksMode = null,
        ?array $languages = null,
        ?string $languagesMode = null,
        ?array $methods = null,
        ?string $methodsMode = null,
        ?array $runtimes = null,
        ?string $runtimesMode = null,
        ?array $oss = null,
        ?string $ossMode = null,
        ?string $distributionCost = null
    ): mixed {
        $request = $this->blackfireServerTopSpansRequest(
            $projectId,
            $environmentId,
            $from,
            $to,
            $grain,
            $sort,
            $contexts,
            $contextsMode,
            $applications,
            $applicationsMode,
            $instances,
            $instancesMode,
            $transactions,
            $transactionsMode,
            $wtSlots,
            $wtSlotsMode,
            $pmuSlots,
            $pmuSlotsMode,
            $httpStatusCodes,
            $httpStatusCodesMode,
            $httpHosts,
            $httpHostsMode,
            $hosts,
            $hostsMode,
            $frameworks,
            $frameworksMode,
            $languages,
            $languagesMode,
            $methods,
            $methodsMode,
            $runtimes,
            $runtimesMode,
            $oss,
            $ossMode,
            $distributionCost
        );

        try {
            $response = $this->sendAuthenticatedRequest(
                $request->getMethod(),
                (string) $request->getUri(),
                $request->getHeaders(),
                $request->getBody()
            );

            return $this->handleResponseWithDataType(
                '\Upsun\Model\BlackfireServerTopSpans200Response',
                $request,
                $response
            );
        } catch (Exception $exception) {
            throw new ApiException(
                sprintf(
                    '[%d] Error connecting to the API (%s)',
                    $exception->getCode(),
                    '/projects/{projectId}/environments/{environmentId}/observability/server/top-spans'
                ),
                $request,
                $response ?? null,
                $exception
            );
        }
    }

    /**
     * Create request for operation 'blackfireServerTopSpans'
     *
     * @param  string $projectId (required)
     * @param  string $environmentId (required)
     * @param  int $from (required)
     * @param  int $to (required)
     * @param  int|null $grain (optional)
     * @param  string|null $sort (optional)
     * @param  string[]|null $contexts (optional)
     * @param  string|null $contextsMode (optional)
     * @param  string[]|null $applications (optional)
     * @param  string|null $applicationsMode (optional)
     * @param  string[]|null $instances (optional)
     * @param  string|null $instancesMode (optional)
     * @param  string[]|null $transactions (optional)
     * @param  string|null $transactionsMode (optional)
     * @param  string[]|null $wtSlots (optional)
     * @param  string|null $wtSlotsMode (optional)
     * @param  string[]|null $pmuSlots (optional)
     * @param  string|null $pmuSlotsMode (optional)
     * @param  string[]|null $httpStatusCodes (optional)
     * @param  string|null $httpStatusCodesMode (optional)
     * @param  string[]|null $httpHosts (optional)
     * @param  string|null $httpHostsMode (optional)
     * @param  string[]|null $hosts (optional)
     * @param  string|null $hostsMode (optional)
     * @param  string[]|null $frameworks (optional)
     * @param  string|null $frameworksMode (optional)
     * @param  string[]|null $languages (optional)
     * @param  string|null $languagesMode (optional)
     * @param  string[]|null $methods (optional)
     * @param  string|null $methodsMode (optional)
     * @param  string[]|null $runtimes (optional)
     * @param  string|null $runtimesMode (optional)
     * @param  string[]|null $oss (optional)
     * @param  string|null $ossMode (optional)
     * @param  string|null $distributionCost (optional)
     *
     * @throws InvalidArgumentException
     */
    private function blackfireServerTopSpansRequest(
        string $projectId,
        string $environmentId,
        int $from,
        int $to,
        ?int $grain = null,
        ?string $sort = null,
        ?array $contexts = null,
        ?string $contextsMode = null,
        ?array $applications = null,
        ?string $applicationsMode = null,
        ?array $instances = null,
        ?string $instancesMode = null,
        ?array $transactions = null,
        ?string $transactionsMode = null,
        ?array $wtSlots = null,
        ?string $wtSlotsMode = null,
        ?array $pmuSlots = null,
        ?string $pmuSlotsMode = null,
        ?array $httpStatusCodes = null,
        ?string $httpStatusCodesMode = null,
        ?array $httpHosts = null,
        ?string $httpHostsMode = null,
        ?array $hosts = null,
        ?string $hostsMode = null,
        ?array $frameworks = null,
        ?string $frameworksMode = null,
        ?array $languages = null,
        ?string $languagesMode = null,
        ?array $methods = null,
        ?string $methodsMode = null,
        ?array $runtimes = null,
        ?string $runtimesMode = null,
        ?array $oss = null,
        ?string $ossMode = null,
        ?string $distributionCost = null
    ): RequestInterface {
        // verify the required parameter 'projectId' is set
        if (empty($projectId)) {
            throw new InvalidArgumentException(
                'Missing the required parameter $projectId
                when calling blackfireServerTopSpans'
            );
        }

        if (!preg_match("/[a-z0-9]+/", $projectId)) {
            throw new InvalidArgumentException(
                "invalid value for \"projectId\" when calling BlackfireMonitoringApi.blackfireServerTopSpans,
                must conform to the pattern /[a-z0-9]+/."
            );
        }

        // verify the required parameter 'environmentId' is set
        if (empty($environmentId)) {
            throw new InvalidArgumentException(
                'Missing the required parameter $environmentId
                when calling blackfireServerTopSpans'
            );
        }

        if (!preg_match("/.+/", $environmentId)) {
            throw new InvalidArgumentException(
                "invalid value for \"environmentId\" when calling BlackfireMonitoringApi.blackfireServerTopSpans,
                must conform to the pattern /.+/."
            );
        }

        // verify the required parameter 'from' is set
        if (empty($from)) {
            throw new InvalidArgumentException(
                'Missing the required parameter $from
                when calling blackfireServerTopSpans'
            );
        }
        // verify the required parameter 'to' is set
        if (empty($to)) {
            throw new InvalidArgumentException(
                'Missing the required parameter $to
                when calling blackfireServerTopSpans'
            );
        }

        $resourcePath = '/projects/{projectId}/environments/{environmentId}/observability/server/top-spans';
        $formParams = [];
        $queryParams = [];
        $headerParams = [];
        $httpBody = null;
        $multipart = false;

        // query params
        if ($from !== null) {
            if ('form' === 'form' && is_array($from)) {
                foreach ($from as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['from'] = $from instanceof DateTime
                    ? $from->format(DATE_ATOM)
                    : ($from);
            }
        }

        // query params
        if ($to !== null) {
            if ('form' === 'form' && is_array($to)) {
                foreach ($to as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['to'] = $to instanceof DateTime
                    ? $to->format(DATE_ATOM)
                    : ($to);
            }
        }

        // query params
        if ($grain !== null) {
            if ('form' === 'form' && is_array($grain)) {
                foreach ($grain as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['grain'] = $grain instanceof DateTime
                    ? $grain->format(DATE_ATOM)
                    : ($grain);
            }
        }

        // query params
        if ($sort !== null) {
            if ('form' === 'form' && is_array($sort)) {
                foreach ($sort as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['sort'] = $sort instanceof DateTime
                    ? $sort->format(DATE_ATOM)
                    : ($sort);
            }
        }

        // query params
        if ($contexts !== null) {
            if ('form' === 'form' && is_array($contexts)) {
                foreach ($contexts as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['contexts[]'] = $contexts instanceof DateTime
                    ? $contexts->format(DATE_ATOM)
                    : ($contexts);
            }
        }

        // query params
        if ($contextsMode !== null) {
            if ('form' === 'form' && is_array($contextsMode)) {
                foreach ($contextsMode as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['contexts_mode'] = $contextsMode instanceof DateTime
                    ? $contextsMode->format(DATE_ATOM)
                    : ($contextsMode);
            }
        }

        // query params
        if ($applications !== null) {
            if ('form' === 'form' && is_array($applications)) {
                foreach ($applications as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['applications[]'] = $applications instanceof DateTime
                    ? $applications->format(DATE_ATOM)
                    : ($applications);
            }
        }

        // query params
        if ($applicationsMode !== null) {
            if ('form' === 'form' && is_array($applicationsMode)) {
                foreach ($applicationsMode as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['applications_mode'] = $applicationsMode instanceof DateTime
                    ? $applicationsMode->format(DATE_ATOM)
                    : ($applicationsMode);
            }
        }

        // query params
        if ($instances !== null) {
            if ('form' === 'form' && is_array($instances)) {
                foreach ($instances as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['instances[]'] = $instances instanceof DateTime
                    ? $instances->format(DATE_ATOM)
                    : ($instances);
            }
        }

        // query params
        if ($instancesMode !== null) {
            if ('form' === 'form' && is_array($instancesMode)) {
                foreach ($instancesMode as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['instances_mode'] = $instancesMode instanceof DateTime
                    ? $instancesMode->format(DATE_ATOM)
                    : ($instancesMode);
            }
        }

        // query params
        if ($transactions !== null) {
            if ('form' === 'form' && is_array($transactions)) {
                foreach ($transactions as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['transactions[]'] = $transactions instanceof DateTime
                    ? $transactions->format(DATE_ATOM)
                    : ($transactions);
            }
        }

        // query params
        if ($transactionsMode !== null) {
            if ('form' === 'form' && is_array($transactionsMode)) {
                foreach ($transactionsMode as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['transactions_mode'] = $transactionsMode instanceof DateTime
                    ? $transactionsMode->format(DATE_ATOM)
                    : ($transactionsMode);
            }
        }

        // query params
        if ($wtSlots !== null) {
            if ('form' === 'form' && is_array($wtSlots)) {
                foreach ($wtSlots as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['wt_slots[]'] = $wtSlots instanceof DateTime
                    ? $wtSlots->format(DATE_ATOM)
                    : ($wtSlots);
            }
        }

        // query params
        if ($wtSlotsMode !== null) {
            if ('form' === 'form' && is_array($wtSlotsMode)) {
                foreach ($wtSlotsMode as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['wt_slots_mode'] = $wtSlotsMode instanceof DateTime
                    ? $wtSlotsMode->format(DATE_ATOM)
                    : ($wtSlotsMode);
            }
        }

        // query params
        if ($pmuSlots !== null) {
            if ('form' === 'form' && is_array($pmuSlots)) {
                foreach ($pmuSlots as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['pmu_slots[]'] = $pmuSlots instanceof DateTime
                    ? $pmuSlots->format(DATE_ATOM)
                    : ($pmuSlots);
            }
        }

        // query params
        if ($pmuSlotsMode !== null) {
            if ('form' === 'form' && is_array($pmuSlotsMode)) {
                foreach ($pmuSlotsMode as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['pmu_slots_mode'] = $pmuSlotsMode instanceof DateTime
                    ? $pmuSlotsMode->format(DATE_ATOM)
                    : ($pmuSlotsMode);
            }
        }

        // query params
        if ($httpStatusCodes !== null) {
            if ('form' === 'form' && is_array($httpStatusCodes)) {
                foreach ($httpStatusCodes as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['http_status_codes[]'] = $httpStatusCodes instanceof DateTime
                    ? $httpStatusCodes->format(DATE_ATOM)
                    : ($httpStatusCodes);
            }
        }

        // query params
        if ($httpStatusCodesMode !== null) {
            if ('form' === 'form' && is_array($httpStatusCodesMode)) {
                foreach ($httpStatusCodesMode as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['http_status_codes_mode'] = $httpStatusCodesMode instanceof DateTime
                    ? $httpStatusCodesMode->format(DATE_ATOM)
                    : ($httpStatusCodesMode);
            }
        }

        // query params
        if ($httpHosts !== null) {
            if ('form' === 'form' && is_array($httpHosts)) {
                foreach ($httpHosts as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['http_hosts[]'] = $httpHosts instanceof DateTime
                    ? $httpHosts->format(DATE_ATOM)
                    : ($httpHosts);
            }
        }

        // query params
        if ($httpHostsMode !== null) {
            if ('form' === 'form' && is_array($httpHostsMode)) {
                foreach ($httpHostsMode as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['http_hosts_mode'] = $httpHostsMode instanceof DateTime
                    ? $httpHostsMode->format(DATE_ATOM)
                    : ($httpHostsMode);
            }
        }

        // query params
        if ($hosts !== null) {
            if ('form' === 'form' && is_array($hosts)) {
                foreach ($hosts as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['hosts[]'] = $hosts instanceof DateTime
                    ? $hosts->format(DATE_ATOM)
                    : ($hosts);
            }
        }

        // query params
        if ($hostsMode !== null) {
            if ('form' === 'form' && is_array($hostsMode)) {
                foreach ($hostsMode as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['hosts_mode'] = $hostsMode instanceof DateTime
                    ? $hostsMode->format(DATE_ATOM)
                    : ($hostsMode);
            }
        }

        // query params
        if ($frameworks !== null) {
            if ('form' === 'form' && is_array($frameworks)) {
                foreach ($frameworks as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['frameworks[]'] = $frameworks instanceof DateTime
                    ? $frameworks->format(DATE_ATOM)
                    : ($frameworks);
            }
        }

        // query params
        if ($frameworksMode !== null) {
            if ('form' === 'form' && is_array($frameworksMode)) {
                foreach ($frameworksMode as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['frameworks_mode'] = $frameworksMode instanceof DateTime
                    ? $frameworksMode->format(DATE_ATOM)
                    : ($frameworksMode);
            }
        }

        // query params
        if ($languages !== null) {
            if ('form' === 'form' && is_array($languages)) {
                foreach ($languages as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['languages[]'] = $languages instanceof DateTime
                    ? $languages->format(DATE_ATOM)
                    : ($languages);
            }
        }

        // query params
        if ($languagesMode !== null) {
            if ('form' === 'form' && is_array($languagesMode)) {
                foreach ($languagesMode as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['languages_mode'] = $languagesMode instanceof DateTime
                    ? $languagesMode->format(DATE_ATOM)
                    : ($languagesMode);
            }
        }

        // query params
        if ($methods !== null) {
            if ('form' === 'form' && is_array($methods)) {
                foreach ($methods as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['methods[]'] = $methods instanceof DateTime
                    ? $methods->format(DATE_ATOM)
                    : ($methods);
            }
        }

        // query params
        if ($methodsMode !== null) {
            if ('form' === 'form' && is_array($methodsMode)) {
                foreach ($methodsMode as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['methods_mode'] = $methodsMode instanceof DateTime
                    ? $methodsMode->format(DATE_ATOM)
                    : ($methodsMode);
            }
        }

        // query params
        if ($runtimes !== null) {
            if ('form' === 'form' && is_array($runtimes)) {
                foreach ($runtimes as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['runtimes[]'] = $runtimes instanceof DateTime
                    ? $runtimes->format(DATE_ATOM)
                    : ($runtimes);
            }
        }

        // query params
        if ($runtimesMode !== null) {
            if ('form' === 'form' && is_array($runtimesMode)) {
                foreach ($runtimesMode as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['runtimes_mode'] = $runtimesMode instanceof DateTime
                    ? $runtimesMode->format(DATE_ATOM)
                    : ($runtimesMode);
            }
        }

        // query params
        if ($oss !== null) {
            if ('form' === 'form' && is_array($oss)) {
                foreach ($oss as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['oss[]'] = $oss instanceof DateTime
                    ? $oss->format(DATE_ATOM)
                    : ($oss);
            }
        }

        // query params
        if ($ossMode !== null) {
            if ('form' === 'form' && is_array($ossMode)) {
                foreach ($ossMode as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['oss_mode'] = $ossMode instanceof DateTime
                    ? $ossMode->format(DATE_ATOM)
                    : ($ossMode);
            }
        }

        // query params
        if ($distributionCost !== null) {
            if ('form' === 'form' && is_array($distributionCost)) {
                foreach ($distributionCost as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['distribution_cost'] = $distributionCost instanceof DateTime
                    ? $distributionCost->format(DATE_ATOM)
                    : ($distributionCost);
            }
        }

        // path params

        if ($projectId !== null) {
            $resourcePath = str_replace(
                '{' . 'projectId' . '}',
                ObjectSerializer::toPathValue($projectId),
                $resourcePath
            );
        }
        // path params

        if ($environmentId !== null) {
            $resourcePath = str_replace(
                '{' . 'environmentId' . '}',
                ObjectSerializer::toPathValue($environmentId),
                $resourcePath
            );
        }

        $headers = $this->headerSelector->selectHeaders(
            ['application/json'],
            '',
            $multipart
        );

        // for model (json/xml)
        if ($formParams !== []) {
            if ($multipart) {
                $multipartContents = [];
                foreach ($formParams as $formParamName => $formParamValue) {
                    $formParamValueItems = is_array($formParamValue) ? $formParamValue : [$formParamValue];
                    foreach ($formParamValueItems as $formParamValueItem) {
                        $multipartContents[] = [
                            'name' => $formParamName,
                            'contents' => $formParamValueItem
                        ];
                    }
                }

                // for HTTP post (form)
                $httpBody = new MultipartStream($multipartContents);
            } elseif ($this->headerSelector->isJsonMime($headers['Content-Type'])) {
                $httpBody = json_encode($formParams);
            } else {
                // for HTTP post (form)
                $httpBody = ObjectSerializer::buildQuery($formParams);
            }
        }

        // this endpoint requires Bearer authentication (access token)
        if ($this->config->getAccessToken() !== null) {
            $headers['Authorization'] = 'Bearer ' . $this->config->getAccessToken();
        }

        $defaultHeaders = [];
        if ($this->config->getUserAgent()) {
            $defaultHeaders['User-Agent'] = $this->config->getUserAgent();
        }
        if ($this->config->getUpsunClientHeader()) {
            $defaultHeaders['X-Upsun-Client'] = $this->config->getUpsunClientHeader();
        }

        $headers = array_merge(
            $defaultHeaders,
            $headerParams,
            $headers
        );

        $operationHost = $this->config->getHost();

        $uri = $this->createUri($operationHost, $resourcePath, $queryParams);

        return $this->createRequest('GET', $uri, $headers, $httpBody);
    }

    /**
     * Get server transaction breakdown
     *
     * Returns transaction breakdown with impact scores, showing how each transaction contributes to overall resource
     * consumption. Includes time-series data for top-hit transactions and aggregated metrics per transaction. Use
     * breakdown_dimension to analyze by wall time (wt), memory (pmu), or stdout. Supports extensive filtering by
     * context, transaction, host, framework, runtime, HTTP method, status code, and more.
     *
     * @param  string $projectId (required)
     * @param  string $environmentId (required)
     * @param  int $from (required)
     * @param  int $to (required)
     * @param  int|null $grain (optional)
     * @param  string|null $breakdownDimension (optional)
     * @param  string|null $sort (optional)
     * @param  int|null $breakdownLimit (optional)
     * @param  string[]|null $contexts (optional)
     * @param  string|null $contextsMode (optional)
     * @param  string[]|null $applications (optional)
     * @param  string|null $applicationsMode (optional)
     * @param  string[]|null $instances (optional)
     * @param  string|null $instancesMode (optional)
     * @param  string[]|null $transactions (optional)
     * @param  string|null $transactionsMode (optional)
     * @param  string[]|null $wtSlots (optional)
     * @param  string|null $wtSlotsMode (optional)
     * @param  string[]|null $pmuSlots (optional)
     * @param  string|null $pmuSlotsMode (optional)
     * @param  string[]|null $httpStatusCodes (optional)
     * @param  string|null $httpStatusCodesMode (optional)
     * @param  string[]|null $httpHosts (optional)
     * @param  string|null $httpHostsMode (optional)
     * @param  string[]|null $hosts (optional)
     * @param  string|null $hostsMode (optional)
     * @param  string[]|null $frameworks (optional)
     * @param  string|null $frameworksMode (optional)
     * @param  string[]|null $languages (optional)
     * @param  string|null $languagesMode (optional)
     * @param  string[]|null $methods (optional)
     * @param  string|null $methodsMode (optional)
     * @param  string[]|null $runtimes (optional)
     * @param  string|null $runtimesMode (optional)
     * @param  string[]|null $oss (optional)
     * @param  string|null $ossMode (optional)
     * @param  string|null $distributionCost (optional)
     *
     * @throws ApiException on non-2xx response or if the response body is not in the expected format
     * @throws ClientExceptionInterface
     * @see https://docs.upsun.com/api/#tag/Blackfire-Monitoring/operation/blackfire_server_transactions_breakdown
     */
    public function blackfireServerTransactionsBreakdown(
        string $projectId,
        string $environmentId,
        int $from,
        int $to,
        ?int $grain = null,
        ?string $breakdownDimension = null,
        ?string $sort = null,
        ?int $breakdownLimit = null,
        ?array $contexts = null,
        ?string $contextsMode = null,
        ?array $applications = null,
        ?string $applicationsMode = null,
        ?array $instances = null,
        ?string $instancesMode = null,
        ?array $transactions = null,
        ?string $transactionsMode = null,
        ?array $wtSlots = null,
        ?string $wtSlotsMode = null,
        ?array $pmuSlots = null,
        ?string $pmuSlotsMode = null,
        ?array $httpStatusCodes = null,
        ?string $httpStatusCodesMode = null,
        ?array $httpHosts = null,
        ?string $httpHostsMode = null,
        ?array $hosts = null,
        ?string $hostsMode = null,
        ?array $frameworks = null,
        ?string $frameworksMode = null,
        ?array $languages = null,
        ?string $languagesMode = null,
        ?array $methods = null,
        ?string $methodsMode = null,
        ?array $runtimes = null,
        ?string $runtimesMode = null,
        ?array $oss = null,
        ?string $ossMode = null,
        ?string $distributionCost = null
    ): mixed {
        return $this->blackfireServerTransactionsBreakdownWithHttpInfo(
            $projectId,
            $environmentId,
            $from,
            $to,
            $grain,
            $breakdownDimension,
            $sort,
            $breakdownLimit,
            $contexts,
            $contextsMode,
            $applications,
            $applicationsMode,
            $instances,
            $instancesMode,
            $transactions,
            $transactionsMode,
            $wtSlots,
            $wtSlotsMode,
            $pmuSlots,
            $pmuSlotsMode,
            $httpStatusCodes,
            $httpStatusCodesMode,
            $httpHosts,
            $httpHostsMode,
            $hosts,
            $hostsMode,
            $frameworks,
            $frameworksMode,
            $languages,
            $languagesMode,
            $methods,
            $methodsMode,
            $runtimes,
            $runtimesMode,
            $oss,
            $ossMode,
            $distributionCost
        );
    }

    /**
     * Get server transaction breakdown with HTTP Info
     *
     * @param  string $projectId (required)
     * @param  string $environmentId (required)
     * @param  int $from (required)
     * @param  int $to (required)
     * @param  int|null $grain (optional)
     * @param  string|null $breakdownDimension (optional)
     * @param  string|null $sort (optional)
     * @param  int|null $breakdownLimit (optional)
     * @param  string[]|null $contexts (optional)
     * @param  string|null $contextsMode (optional)
     * @param  string[]|null $applications (optional)
     * @param  string|null $applicationsMode (optional)
     * @param  string[]|null $instances (optional)
     * @param  string|null $instancesMode (optional)
     * @param  string[]|null $transactions (optional)
     * @param  string|null $transactionsMode (optional)
     * @param  string[]|null $wtSlots (optional)
     * @param  string|null $wtSlotsMode (optional)
     * @param  string[]|null $pmuSlots (optional)
     * @param  string|null $pmuSlotsMode (optional)
     * @param  string[]|null $httpStatusCodes (optional)
     * @param  string|null $httpStatusCodesMode (optional)
     * @param  string[]|null $httpHosts (optional)
     * @param  string|null $httpHostsMode (optional)
     * @param  string[]|null $hosts (optional)
     * @param  string|null $hostsMode (optional)
     * @param  string[]|null $frameworks (optional)
     * @param  string|null $frameworksMode (optional)
     * @param  string[]|null $languages (optional)
     * @param  string|null $languagesMode (optional)
     * @param  string[]|null $methods (optional)
     * @param  string|null $methodsMode (optional)
     * @param  string[]|null $runtimes (optional)
     * @param  string|null $runtimesMode (optional)
     * @param  string[]|null $oss (optional)
     * @param  string|null $ossMode (optional)
     * @param  string|null $distributionCost (optional)
     *
     * @throws ApiException on non-2xx response or if the response body is not in the expected format
     * @throws ClientExceptionInterface
    */
    private function blackfireServerTransactionsBreakdownWithHttpInfo(
        string $projectId,
        string $environmentId,
        int $from,
        int $to,
        ?int $grain = null,
        ?string $breakdownDimension = null,
        ?string $sort = null,
        ?int $breakdownLimit = null,
        ?array $contexts = null,
        ?string $contextsMode = null,
        ?array $applications = null,
        ?string $applicationsMode = null,
        ?array $instances = null,
        ?string $instancesMode = null,
        ?array $transactions = null,
        ?string $transactionsMode = null,
        ?array $wtSlots = null,
        ?string $wtSlotsMode = null,
        ?array $pmuSlots = null,
        ?string $pmuSlotsMode = null,
        ?array $httpStatusCodes = null,
        ?string $httpStatusCodesMode = null,
        ?array $httpHosts = null,
        ?string $httpHostsMode = null,
        ?array $hosts = null,
        ?string $hostsMode = null,
        ?array $frameworks = null,
        ?string $frameworksMode = null,
        ?array $languages = null,
        ?string $languagesMode = null,
        ?array $methods = null,
        ?string $methodsMode = null,
        ?array $runtimes = null,
        ?string $runtimesMode = null,
        ?array $oss = null,
        ?string $ossMode = null,
        ?string $distributionCost = null
    ): mixed {
        $request = $this->blackfireServerTransactionsBreakdownRequest(
            $projectId,
            $environmentId,
            $from,
            $to,
            $grain,
            $breakdownDimension,
            $sort,
            $breakdownLimit,
            $contexts,
            $contextsMode,
            $applications,
            $applicationsMode,
            $instances,
            $instancesMode,
            $transactions,
            $transactionsMode,
            $wtSlots,
            $wtSlotsMode,
            $pmuSlots,
            $pmuSlotsMode,
            $httpStatusCodes,
            $httpStatusCodesMode,
            $httpHosts,
            $httpHostsMode,
            $hosts,
            $hostsMode,
            $frameworks,
            $frameworksMode,
            $languages,
            $languagesMode,
            $methods,
            $methodsMode,
            $runtimes,
            $runtimesMode,
            $oss,
            $ossMode,
            $distributionCost
        );

        try {
            $response = $this->sendAuthenticatedRequest(
                $request->getMethod(),
                (string) $request->getUri(),
                $request->getHeaders(),
                $request->getBody()
            );

            return $this->handleResponseWithDataType(
                '\Upsun\Model\BlackfireServerTransactionsBreakdown200Response',
                $request,
                $response
            );
        } catch (Exception $exception) {
            throw new ApiException(
                sprintf(
                    '[%d] Error connecting to the API (%s)',
                    $exception->getCode(),
                    '/projects/{projectId}/environments/{environmentId}/observability/server/transactions-break-down'
                ),
                $request,
                $response ?? null,
                $exception
            );
        }
    }

    /**
     * Create request for operation 'blackfireServerTransactionsBreakdown'
     *
     * @param  string $projectId (required)
     * @param  string $environmentId (required)
     * @param  int $from (required)
     * @param  int $to (required)
     * @param  int|null $grain (optional)
     * @param  string|null $breakdownDimension (optional)
     * @param  string|null $sort (optional)
     * @param  int|null $breakdownLimit (optional)
     * @param  string[]|null $contexts (optional)
     * @param  string|null $contextsMode (optional)
     * @param  string[]|null $applications (optional)
     * @param  string|null $applicationsMode (optional)
     * @param  string[]|null $instances (optional)
     * @param  string|null $instancesMode (optional)
     * @param  string[]|null $transactions (optional)
     * @param  string|null $transactionsMode (optional)
     * @param  string[]|null $wtSlots (optional)
     * @param  string|null $wtSlotsMode (optional)
     * @param  string[]|null $pmuSlots (optional)
     * @param  string|null $pmuSlotsMode (optional)
     * @param  string[]|null $httpStatusCodes (optional)
     * @param  string|null $httpStatusCodesMode (optional)
     * @param  string[]|null $httpHosts (optional)
     * @param  string|null $httpHostsMode (optional)
     * @param  string[]|null $hosts (optional)
     * @param  string|null $hostsMode (optional)
     * @param  string[]|null $frameworks (optional)
     * @param  string|null $frameworksMode (optional)
     * @param  string[]|null $languages (optional)
     * @param  string|null $languagesMode (optional)
     * @param  string[]|null $methods (optional)
     * @param  string|null $methodsMode (optional)
     * @param  string[]|null $runtimes (optional)
     * @param  string|null $runtimesMode (optional)
     * @param  string[]|null $oss (optional)
     * @param  string|null $ossMode (optional)
     * @param  string|null $distributionCost (optional)
     *
     * @throws InvalidArgumentException
     */
    private function blackfireServerTransactionsBreakdownRequest(
        string $projectId,
        string $environmentId,
        int $from,
        int $to,
        ?int $grain = null,
        ?string $breakdownDimension = null,
        ?string $sort = null,
        ?int $breakdownLimit = null,
        ?array $contexts = null,
        ?string $contextsMode = null,
        ?array $applications = null,
        ?string $applicationsMode = null,
        ?array $instances = null,
        ?string $instancesMode = null,
        ?array $transactions = null,
        ?string $transactionsMode = null,
        ?array $wtSlots = null,
        ?string $wtSlotsMode = null,
        ?array $pmuSlots = null,
        ?string $pmuSlotsMode = null,
        ?array $httpStatusCodes = null,
        ?string $httpStatusCodesMode = null,
        ?array $httpHosts = null,
        ?string $httpHostsMode = null,
        ?array $hosts = null,
        ?string $hostsMode = null,
        ?array $frameworks = null,
        ?string $frameworksMode = null,
        ?array $languages = null,
        ?string $languagesMode = null,
        ?array $methods = null,
        ?string $methodsMode = null,
        ?array $runtimes = null,
        ?string $runtimesMode = null,
        ?array $oss = null,
        ?string $ossMode = null,
        ?string $distributionCost = null
    ): RequestInterface {
        // verify the required parameter 'projectId' is set
        if (empty($projectId)) {
            throw new InvalidArgumentException(
                'Missing the required parameter $projectId
                when calling blackfireServerTransactionsBreakdown'
            );
        }

        if (!preg_match("/[a-z0-9]+/", $projectId)) {
            throw new InvalidArgumentException(
                "invalid value for \"projectId\" when calling BlackfireMonitoringApi.blackfireServerTransactionsBreakdown,
                must conform to the pattern /[a-z0-9]+/."
            );
        }

        // verify the required parameter 'environmentId' is set
        if (empty($environmentId)) {
            throw new InvalidArgumentException(
                'Missing the required parameter $environmentId
                when calling blackfireServerTransactionsBreakdown'
            );
        }

        if (!preg_match("/.+/", $environmentId)) {
            throw new InvalidArgumentException(
                "invalid value for \"environmentId\" when calling BlackfireMonitoringApi.blackfireServerTransactionsBreakdown,
                must conform to the pattern /.+/."
            );
        }

        // verify the required parameter 'from' is set
        if (empty($from)) {
            throw new InvalidArgumentException(
                'Missing the required parameter $from
                when calling blackfireServerTransactionsBreakdown'
            );
        }
        // verify the required parameter 'to' is set
        if (empty($to)) {
            throw new InvalidArgumentException(
                'Missing the required parameter $to
                when calling blackfireServerTransactionsBreakdown'
            );
        }

        if ($breakdownLimit !== null && $breakdownLimit > 300) {
            throw new InvalidArgumentException(
                'invalid value for "$breakdownLimit" when calling BlackfireMonitoringApi.blackfireServerTransactionsBreakdown,
                must be smaller than or equal to 300.'
            );
        }

        if ($breakdownLimit !== null && $breakdownLimit < 1) {
            throw new InvalidArgumentException(
                'invalid value for "$breakdownLimit" when calling BlackfireMonitoringApi.blackfireServerTransactionsBreakdown,
                must be bigger than or equal to 1.'
            );
        }

        $resourcePath = '/projects/{projectId}/environments/{environmentId}/observability/server/transactions-break-down';
        $formParams = [];
        $queryParams = [];
        $headerParams = [];
        $httpBody = null;
        $multipart = false;

        // query params
        if ($from !== null) {
            if ('form' === 'form' && is_array($from)) {
                foreach ($from as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['from'] = $from instanceof DateTime
                    ? $from->format(DATE_ATOM)
                    : ($from);
            }
        }

        // query params
        if ($to !== null) {
            if ('form' === 'form' && is_array($to)) {
                foreach ($to as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['to'] = $to instanceof DateTime
                    ? $to->format(DATE_ATOM)
                    : ($to);
            }
        }

        // query params
        if ($grain !== null) {
            if ('form' === 'form' && is_array($grain)) {
                foreach ($grain as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['grain'] = $grain instanceof DateTime
                    ? $grain->format(DATE_ATOM)
                    : ($grain);
            }
        }

        // query params
        if ($breakdownDimension !== null) {
            if ('form' === 'form' && is_array($breakdownDimension)) {
                foreach ($breakdownDimension as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['breakdown_dimension'] = $breakdownDimension instanceof DateTime
                    ? $breakdownDimension->format(DATE_ATOM)
                    : ($breakdownDimension);
            }
        }

        // query params
        if ($sort !== null) {
            if ('form' === 'form' && is_array($sort)) {
                foreach ($sort as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['sort'] = $sort instanceof DateTime
                    ? $sort->format(DATE_ATOM)
                    : ($sort);
            }
        }

        // query params
        if ($breakdownLimit !== null) {
            if ('form' === 'form' && is_array($breakdownLimit)) {
                foreach ($breakdownLimit as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['breakdown_limit'] = $breakdownLimit instanceof DateTime
                    ? $breakdownLimit->format(DATE_ATOM)
                    : ($breakdownLimit);
            }
        }

        // query params
        if ($contexts !== null) {
            if ('form' === 'form' && is_array($contexts)) {
                foreach ($contexts as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['contexts[]'] = $contexts instanceof DateTime
                    ? $contexts->format(DATE_ATOM)
                    : ($contexts);
            }
        }

        // query params
        if ($contextsMode !== null) {
            if ('form' === 'form' && is_array($contextsMode)) {
                foreach ($contextsMode as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['contexts_mode'] = $contextsMode instanceof DateTime
                    ? $contextsMode->format(DATE_ATOM)
                    : ($contextsMode);
            }
        }

        // query params
        if ($applications !== null) {
            if ('form' === 'form' && is_array($applications)) {
                foreach ($applications as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['applications[]'] = $applications instanceof DateTime
                    ? $applications->format(DATE_ATOM)
                    : ($applications);
            }
        }

        // query params
        if ($applicationsMode !== null) {
            if ('form' === 'form' && is_array($applicationsMode)) {
                foreach ($applicationsMode as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['applications_mode'] = $applicationsMode instanceof DateTime
                    ? $applicationsMode->format(DATE_ATOM)
                    : ($applicationsMode);
            }
        }

        // query params
        if ($instances !== null) {
            if ('form' === 'form' && is_array($instances)) {
                foreach ($instances as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['instances[]'] = $instances instanceof DateTime
                    ? $instances->format(DATE_ATOM)
                    : ($instances);
            }
        }

        // query params
        if ($instancesMode !== null) {
            if ('form' === 'form' && is_array($instancesMode)) {
                foreach ($instancesMode as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['instances_mode'] = $instancesMode instanceof DateTime
                    ? $instancesMode->format(DATE_ATOM)
                    : ($instancesMode);
            }
        }

        // query params
        if ($transactions !== null) {
            if ('form' === 'form' && is_array($transactions)) {
                foreach ($transactions as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['transactions[]'] = $transactions instanceof DateTime
                    ? $transactions->format(DATE_ATOM)
                    : ($transactions);
            }
        }

        // query params
        if ($transactionsMode !== null) {
            if ('form' === 'form' && is_array($transactionsMode)) {
                foreach ($transactionsMode as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['transactions_mode'] = $transactionsMode instanceof DateTime
                    ? $transactionsMode->format(DATE_ATOM)
                    : ($transactionsMode);
            }
        }

        // query params
        if ($wtSlots !== null) {
            if ('form' === 'form' && is_array($wtSlots)) {
                foreach ($wtSlots as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['wt_slots[]'] = $wtSlots instanceof DateTime
                    ? $wtSlots->format(DATE_ATOM)
                    : ($wtSlots);
            }
        }

        // query params
        if ($wtSlotsMode !== null) {
            if ('form' === 'form' && is_array($wtSlotsMode)) {
                foreach ($wtSlotsMode as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['wt_slots_mode'] = $wtSlotsMode instanceof DateTime
                    ? $wtSlotsMode->format(DATE_ATOM)
                    : ($wtSlotsMode);
            }
        }

        // query params
        if ($pmuSlots !== null) {
            if ('form' === 'form' && is_array($pmuSlots)) {
                foreach ($pmuSlots as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['pmu_slots[]'] = $pmuSlots instanceof DateTime
                    ? $pmuSlots->format(DATE_ATOM)
                    : ($pmuSlots);
            }
        }

        // query params
        if ($pmuSlotsMode !== null) {
            if ('form' === 'form' && is_array($pmuSlotsMode)) {
                foreach ($pmuSlotsMode as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['pmu_slots_mode'] = $pmuSlotsMode instanceof DateTime
                    ? $pmuSlotsMode->format(DATE_ATOM)
                    : ($pmuSlotsMode);
            }
        }

        // query params
        if ($httpStatusCodes !== null) {
            if ('form' === 'form' && is_array($httpStatusCodes)) {
                foreach ($httpStatusCodes as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['http_status_codes[]'] = $httpStatusCodes instanceof DateTime
                    ? $httpStatusCodes->format(DATE_ATOM)
                    : ($httpStatusCodes);
            }
        }

        // query params
        if ($httpStatusCodesMode !== null) {
            if ('form' === 'form' && is_array($httpStatusCodesMode)) {
                foreach ($httpStatusCodesMode as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['http_status_codes_mode'] = $httpStatusCodesMode instanceof DateTime
                    ? $httpStatusCodesMode->format(DATE_ATOM)
                    : ($httpStatusCodesMode);
            }
        }

        // query params
        if ($httpHosts !== null) {
            if ('form' === 'form' && is_array($httpHosts)) {
                foreach ($httpHosts as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['http_hosts[]'] = $httpHosts instanceof DateTime
                    ? $httpHosts->format(DATE_ATOM)
                    : ($httpHosts);
            }
        }

        // query params
        if ($httpHostsMode !== null) {
            if ('form' === 'form' && is_array($httpHostsMode)) {
                foreach ($httpHostsMode as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['http_hosts_mode'] = $httpHostsMode instanceof DateTime
                    ? $httpHostsMode->format(DATE_ATOM)
                    : ($httpHostsMode);
            }
        }

        // query params
        if ($hosts !== null) {
            if ('form' === 'form' && is_array($hosts)) {
                foreach ($hosts as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['hosts[]'] = $hosts instanceof DateTime
                    ? $hosts->format(DATE_ATOM)
                    : ($hosts);
            }
        }

        // query params
        if ($hostsMode !== null) {
            if ('form' === 'form' && is_array($hostsMode)) {
                foreach ($hostsMode as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['hosts_mode'] = $hostsMode instanceof DateTime
                    ? $hostsMode->format(DATE_ATOM)
                    : ($hostsMode);
            }
        }

        // query params
        if ($frameworks !== null) {
            if ('form' === 'form' && is_array($frameworks)) {
                foreach ($frameworks as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['frameworks[]'] = $frameworks instanceof DateTime
                    ? $frameworks->format(DATE_ATOM)
                    : ($frameworks);
            }
        }

        // query params
        if ($frameworksMode !== null) {
            if ('form' === 'form' && is_array($frameworksMode)) {
                foreach ($frameworksMode as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['frameworks_mode'] = $frameworksMode instanceof DateTime
                    ? $frameworksMode->format(DATE_ATOM)
                    : ($frameworksMode);
            }
        }

        // query params
        if ($languages !== null) {
            if ('form' === 'form' && is_array($languages)) {
                foreach ($languages as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['languages[]'] = $languages instanceof DateTime
                    ? $languages->format(DATE_ATOM)
                    : ($languages);
            }
        }

        // query params
        if ($languagesMode !== null) {
            if ('form' === 'form' && is_array($languagesMode)) {
                foreach ($languagesMode as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['languages_mode'] = $languagesMode instanceof DateTime
                    ? $languagesMode->format(DATE_ATOM)
                    : ($languagesMode);
            }
        }

        // query params
        if ($methods !== null) {
            if ('form' === 'form' && is_array($methods)) {
                foreach ($methods as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['methods[]'] = $methods instanceof DateTime
                    ? $methods->format(DATE_ATOM)
                    : ($methods);
            }
        }

        // query params
        if ($methodsMode !== null) {
            if ('form' === 'form' && is_array($methodsMode)) {
                foreach ($methodsMode as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['methods_mode'] = $methodsMode instanceof DateTime
                    ? $methodsMode->format(DATE_ATOM)
                    : ($methodsMode);
            }
        }

        // query params
        if ($runtimes !== null) {
            if ('form' === 'form' && is_array($runtimes)) {
                foreach ($runtimes as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['runtimes[]'] = $runtimes instanceof DateTime
                    ? $runtimes->format(DATE_ATOM)
                    : ($runtimes);
            }
        }

        // query params
        if ($runtimesMode !== null) {
            if ('form' === 'form' && is_array($runtimesMode)) {
                foreach ($runtimesMode as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['runtimes_mode'] = $runtimesMode instanceof DateTime
                    ? $runtimesMode->format(DATE_ATOM)
                    : ($runtimesMode);
            }
        }

        // query params
        if ($oss !== null) {
            if ('form' === 'form' && is_array($oss)) {
                foreach ($oss as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['oss[]'] = $oss instanceof DateTime
                    ? $oss->format(DATE_ATOM)
                    : ($oss);
            }
        }

        // query params
        if ($ossMode !== null) {
            if ('form' === 'form' && is_array($ossMode)) {
                foreach ($ossMode as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['oss_mode'] = $ossMode instanceof DateTime
                    ? $ossMode->format(DATE_ATOM)
                    : ($ossMode);
            }
        }

        // query params
        if ($distributionCost !== null) {
            if ('form' === 'form' && is_array($distributionCost)) {
                foreach ($distributionCost as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['distribution_cost'] = $distributionCost instanceof DateTime
                    ? $distributionCost->format(DATE_ATOM)
                    : ($distributionCost);
            }
        }

        // path params

        if ($projectId !== null) {
            $resourcePath = str_replace(
                '{' . 'projectId' . '}',
                ObjectSerializer::toPathValue($projectId),
                $resourcePath
            );
        }
        // path params

        if ($environmentId !== null) {
            $resourcePath = str_replace(
                '{' . 'environmentId' . '}',
                ObjectSerializer::toPathValue($environmentId),
                $resourcePath
            );
        }

        $headers = $this->headerSelector->selectHeaders(
            ['application/json'],
            '',
            $multipart
        );

        // for model (json/xml)
        if ($formParams !== []) {
            if ($multipart) {
                $multipartContents = [];
                foreach ($formParams as $formParamName => $formParamValue) {
                    $formParamValueItems = is_array($formParamValue) ? $formParamValue : [$formParamValue];
                    foreach ($formParamValueItems as $formParamValueItem) {
                        $multipartContents[] = [
                            'name' => $formParamName,
                            'contents' => $formParamValueItem
                        ];
                    }
                }

                // for HTTP post (form)
                $httpBody = new MultipartStream($multipartContents);
            } elseif ($this->headerSelector->isJsonMime($headers['Content-Type'])) {
                $httpBody = json_encode($formParams);
            } else {
                // for HTTP post (form)
                $httpBody = ObjectSerializer::buildQuery($formParams);
            }
        }

        // this endpoint requires Bearer authentication (access token)
        if ($this->config->getAccessToken() !== null) {
            $headers['Authorization'] = 'Bearer ' . $this->config->getAccessToken();
        }

        $defaultHeaders = [];
        if ($this->config->getUserAgent()) {
            $defaultHeaders['User-Agent'] = $this->config->getUserAgent();
        }
        if ($this->config->getUpsunClientHeader()) {
            $defaultHeaders['X-Upsun-Client'] = $this->config->getUpsunClientHeader();
        }

        $headers = array_merge(
            $defaultHeaders,
            $headerParams,
            $headers
        );

        $operationHost = $this->config->getHost();

        $uri = $this->createUri($operationHost, $resourcePath, $queryParams);

        return $this->createRequest('GET', $uri, $headers, $httpBody);
    }
}
