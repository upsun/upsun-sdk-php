<?php

namespace Upsun\Api;

use DateTime;
use Exception;
use GuzzleHttp\Psr7\MultipartStream;
use InvalidArgumentException;
use Psr\Http\Client\ClientExceptionInterface;
use Psr\Http\Client\ClientInterface;
use Psr\Http\Message\RequestFactoryInterface;
use Psr\Http\Message\RequestInterface;
use Psr\Http\Message\StreamFactoryInterface;
use Upsun\Api\Serializer\ObjectSerializer;
use Upsun\Core\OAuthProvider;

/**
 * Low level ContinuousProfilingApi (auto-generated)
 *
 * @license   Apache-2.0
 * @see       https://docs.upsun.com
 * @generated This file was generated by OpenAPI Generator. Do not edit manually.
 */
final class ContinuousProfilingApi extends AbstractApi
{
    private readonly ApiHeaderSelector $headerSelector;

    private APIConfiguration $config;

    public function __construct(
        OAuthProvider $oauthProvider,
        ?ClientInterface $httpClient = null,
        ?RequestFactoryInterface $requestFactory = null,
        ?APIConfiguration $config = null,
        ?StreamFactoryInterface $streamFactory = null,
        ?ApiHeaderSelector $selector = null,
    ) {
        parent::__construct(
            $oauthProvider,
            $httpClient,
            $requestFactory,
            AbstractApi::BASE_PATH,
            $streamFactory
        );

        $this->config = $config ?? (new APIConfiguration())->setHost(AbstractApi::BASE_PATH);

        $this->headerSelector = $selector ?? new ApiHeaderSelector();
    }

    /**
     * Get application filter values
     *
     * Returns available label filter fields and their distinct values for the given application. Use these values to
     * build filter parameters (`runtime[]`, `runtime_version[]`, etc.) on the timeline and merge endpoints
     *
     * @param  string $projectId (required)
     * @param  string $envId (required)
     * @param  string $app (required)
     * @param  int|null $from (optional)
     * @param  int|null $to (optional)
     * @param  string|null $profileType (optional)
     * @param  int|null $runtimeMode
     *         Filter mode for runtime parameter. "1" (additive) includes only specified
     *         runtimes. "-1" (subtractive) excludes specified runtimes and includes all
     *         others. When omitted while runtime[] is supplied, defaults to "1" (additive). (optional)
     * @param  string[]|null $runtime
     *         Filter by runtime language. Can be specified multiple times for multiple values
     *         (e.g., ?runtime[]=go&runtime[]=python). Use runtime_mode to control
     *         inclusion/exclusion behavior. (optional)
     * @param  int|null $runtimeVersionMode
     *         Filter mode for runtime_version parameter. "1" (additive) includes only
     *         specified versions. "-1" (subtractive) excludes specified versions and includes
     *         all others. When omitted while runtime_version[] is supplied, defaults to "1"
     *         (additive). (optional)
     * @param  string[]|null $runtimeVersion
     *         Filter by runtime version. Can be specified multiple times for multiple values
     *         (e.g., ?runtime_version[]=1.21&runtime_version[]=1.22). Use runtime_version_mode
     *         to control inclusion/exclusion behavior. (optional)
     * @param  int|null $runtimeArchMode
     *         Filter mode for runtime_arch parameter. "1" (additive) includes only specified
     *         architectures. "-1" (subtractive) excludes specified architectures and includes
     *         all others. When omitted while runtime_arch[] is supplied, defaults to "1"
     *         (additive). (optional)
     * @param  string[]|null $runtimeArch
     *         Filter by runtime architecture. Can be specified multiple times for multiple
     *         values (e.g., ?runtime_arch[]=amd64&runtime_arch[]=arm64). Use runtime_arch_mode
     *         to control inclusion/exclusion behavior. (optional)
     * @param  int|null $runtimeOsMode
     *         Filter mode for runtime_os parameter. "1" (additive) includes only specified
     *         operating systems. "-1" (subtractive) excludes specified operating systems and
     *         includes all others. When omitted while runtime_os[] is supplied, defaults to
     *         "1" (additive). (optional)
     * @param  string[]|null $runtimeOs
     *         Filter by runtime operating system. Can be specified multiple times for multiple
     *         values (e.g., ?runtime_os[]=linux&runtime_os[]=darwin). Use runtime_os_mode to
     *         control inclusion/exclusion behavior. (optional)
     * @param  int|null $probeVersionMode
     *         Filter mode for probe_version parameter. "1" (additive) includes only specified
     *         probe versions. "-1" (subtractive) excludes specified probe versions and
     *         includes all others. When omitted while probe_version[] is supplied, defaults to
     *         "1" (additive). (optional)
     * @param  string[]|null $probeVersion
     *         Filter by probe version. Can be specified multiple times for multiple values
     *         (e.g., ?probe_version[]=2.28.2&probe_version[]=2.28.1). Use probe_version_mode
     *         to control inclusion/exclusion behavior. (optional)
     *
     * @throws ApiException on non-2xx response or if the response body is not in the expected format
     * @throws ClientExceptionInterface
     * @see https://docs.upsun.com/api/#tag/Continuous-Profiling/operation/get_application_filter
     */
    public function getApplicationFilter(
        string $projectId,
        string $envId,
        string $app,
        ?int $from = null,
        ?int $to = null,
        ?string $profileType = null,
        ?int $runtimeMode = null,
        ?array $runtime = null,
        ?int $runtimeVersionMode = null,
        ?array $runtimeVersion = null,
        ?int $runtimeArchMode = null,
        ?array $runtimeArch = null,
        ?int $runtimeOsMode = null,
        ?array $runtimeOs = null,
        ?int $probeVersionMode = null,
        ?array $probeVersion = null
    ): mixed {
        return $this->getApplicationFilterWithHttpInfo(
            $projectId,
            $envId,
            $app,
            $from,
            $to,
            $profileType,
            $runtimeMode,
            $runtime,
            $runtimeVersionMode,
            $runtimeVersion,
            $runtimeArchMode,
            $runtimeArch,
            $runtimeOsMode,
            $runtimeOs,
            $probeVersionMode,
            $probeVersion
        );
    }

    /**
     * Get application filter values with HTTP Info
     *
     * @param  string $projectId (required)
     * @param  string $envId (required)
     * @param  string $app (required)
     * @param  int|null $from (optional)
     * @param  int|null $to (optional)
     * @param  string|null $profileType (optional)
     * @param  int|null $runtimeMode
     *         Filter mode for runtime parameter. "1" (additive) includes only specified
     *         runtimes. "-1" (subtractive) excludes specified runtimes and includes all
     *         others. When omitted while runtime[] is supplied, defaults to "1" (additive). (optional)
     * @param  string[]|null $runtime
     *         Filter by runtime language. Can be specified multiple times for multiple values
     *         (e.g., ?runtime[]=go&runtime[]=python). Use runtime_mode to control
     *         inclusion/exclusion behavior. (optional)
     * @param  int|null $runtimeVersionMode
     *         Filter mode for runtime_version parameter. "1" (additive) includes only
     *         specified versions. "-1" (subtractive) excludes specified versions and includes
     *         all others. When omitted while runtime_version[] is supplied, defaults to "1"
     *         (additive). (optional)
     * @param  string[]|null $runtimeVersion
     *         Filter by runtime version. Can be specified multiple times for multiple values
     *         (e.g., ?runtime_version[]=1.21&runtime_version[]=1.22). Use runtime_version_mode
     *         to control inclusion/exclusion behavior. (optional)
     * @param  int|null $runtimeArchMode
     *         Filter mode for runtime_arch parameter. "1" (additive) includes only specified
     *         architectures. "-1" (subtractive) excludes specified architectures and includes
     *         all others. When omitted while runtime_arch[] is supplied, defaults to "1"
     *         (additive). (optional)
     * @param  string[]|null $runtimeArch
     *         Filter by runtime architecture. Can be specified multiple times for multiple
     *         values (e.g., ?runtime_arch[]=amd64&runtime_arch[]=arm64). Use runtime_arch_mode
     *         to control inclusion/exclusion behavior. (optional)
     * @param  int|null $runtimeOsMode
     *         Filter mode for runtime_os parameter. "1" (additive) includes only specified
     *         operating systems. "-1" (subtractive) excludes specified operating systems and
     *         includes all others. When omitted while runtime_os[] is supplied, defaults to
     *         "1" (additive). (optional)
     * @param  string[]|null $runtimeOs
     *         Filter by runtime operating system. Can be specified multiple times for multiple
     *         values (e.g., ?runtime_os[]=linux&runtime_os[]=darwin). Use runtime_os_mode to
     *         control inclusion/exclusion behavior. (optional)
     * @param  int|null $probeVersionMode
     *         Filter mode for probe_version parameter. "1" (additive) includes only specified
     *         probe versions. "-1" (subtractive) excludes specified probe versions and
     *         includes all others. When omitted while probe_version[] is supplied, defaults to
     *         "1" (additive). (optional)
     * @param  string[]|null $probeVersion
     *         Filter by probe version. Can be specified multiple times for multiple values
     *         (e.g., ?probe_version[]=2.28.2&probe_version[]=2.28.1). Use probe_version_mode
     *         to control inclusion/exclusion behavior. (optional)
     *
     * @throws ApiException on non-2xx response or if the response body is not in the expected format
     * @throws ClientExceptionInterface
    */
    private function getApplicationFilterWithHttpInfo(
        string $projectId,
        string $envId,
        string $app,
        ?int $from = null,
        ?int $to = null,
        ?string $profileType = null,
        ?int $runtimeMode = null,
        ?array $runtime = null,
        ?int $runtimeVersionMode = null,
        ?array $runtimeVersion = null,
        ?int $runtimeArchMode = null,
        ?array $runtimeArch = null,
        ?int $runtimeOsMode = null,
        ?array $runtimeOs = null,
        ?int $probeVersionMode = null,
        ?array $probeVersion = null
    ): mixed {
        $request = $this->getApplicationFilterRequest(
            $projectId,
            $envId,
            $app,
            $from,
            $to,
            $profileType,
            $runtimeMode,
            $runtime,
            $runtimeVersionMode,
            $runtimeVersion,
            $runtimeArchMode,
            $runtimeArch,
            $runtimeOsMode,
            $runtimeOs,
            $probeVersionMode,
            $probeVersion
        );

        try {
            $response = $this->sendAuthenticatedRequest(
                $request->getMethod(),
                (string) $request->getUri(),
                $request->getHeaders(),
                $request->getBody()
            );

            return $this->handleResponseWithDataType(
                '\Upsun\Model\GetApplicationFilter200Response',
                $request,
                $response
            );
        } catch (Exception $exception) {
            throw new ApiException(
                sprintf(
                    '[%d] Error connecting to the API (%s)',
                    $exception->getCode(),
                    '/projects/{projectId}/environments/{envId}/continuous-profiling/app/{app}/filter'
                ),
                $request,
                $response ?? null,
                $exception
            );
        }
    }

    /**
     * Create request for operation 'getApplicationFilter'
     *
     * @param  string $projectId (required)
     * @param  string $envId (required)
     * @param  string $app (required)
     * @param  int|null $from (optional)
     * @param  int|null $to (optional)
     * @param  string|null $profileType (optional)
     * @param  int|null $runtimeMode
     *         Filter mode for runtime parameter. "1" (additive) includes only specified
     *         runtimes. "-1" (subtractive) excludes specified runtimes and includes all
     *         others. When omitted while runtime[] is supplied, defaults to "1" (additive). (optional)
     * @param  string[]|null $runtime
     *         Filter by runtime language. Can be specified multiple times for multiple values
     *         (e.g., ?runtime[]=go&runtime[]=python). Use runtime_mode to control
     *         inclusion/exclusion behavior. (optional)
     * @param  int|null $runtimeVersionMode
     *         Filter mode for runtime_version parameter. "1" (additive) includes only
     *         specified versions. "-1" (subtractive) excludes specified versions and includes
     *         all others. When omitted while runtime_version[] is supplied, defaults to "1"
     *         (additive). (optional)
     * @param  string[]|null $runtimeVersion
     *         Filter by runtime version. Can be specified multiple times for multiple values
     *         (e.g., ?runtime_version[]=1.21&runtime_version[]=1.22). Use runtime_version_mode
     *         to control inclusion/exclusion behavior. (optional)
     * @param  int|null $runtimeArchMode
     *         Filter mode for runtime_arch parameter. "1" (additive) includes only specified
     *         architectures. "-1" (subtractive) excludes specified architectures and includes
     *         all others. When omitted while runtime_arch[] is supplied, defaults to "1"
     *         (additive). (optional)
     * @param  string[]|null $runtimeArch
     *         Filter by runtime architecture. Can be specified multiple times for multiple
     *         values (e.g., ?runtime_arch[]=amd64&runtime_arch[]=arm64). Use runtime_arch_mode
     *         to control inclusion/exclusion behavior. (optional)
     * @param  int|null $runtimeOsMode
     *         Filter mode for runtime_os parameter. "1" (additive) includes only specified
     *         operating systems. "-1" (subtractive) excludes specified operating systems and
     *         includes all others. When omitted while runtime_os[] is supplied, defaults to
     *         "1" (additive). (optional)
     * @param  string[]|null $runtimeOs
     *         Filter by runtime operating system. Can be specified multiple times for multiple
     *         values (e.g., ?runtime_os[]=linux&runtime_os[]=darwin). Use runtime_os_mode to
     *         control inclusion/exclusion behavior. (optional)
     * @param  int|null $probeVersionMode
     *         Filter mode for probe_version parameter. "1" (additive) includes only specified
     *         probe versions. "-1" (subtractive) excludes specified probe versions and
     *         includes all others. When omitted while probe_version[] is supplied, defaults to
     *         "1" (additive). (optional)
     * @param  string[]|null $probeVersion
     *         Filter by probe version. Can be specified multiple times for multiple values
     *         (e.g., ?probe_version[]=2.28.2&probe_version[]=2.28.1). Use probe_version_mode
     *         to control inclusion/exclusion behavior. (optional)
     *
     * @throws InvalidArgumentException
     */
    private function getApplicationFilterRequest(
        string $projectId,
        string $envId,
        string $app,
        ?int $from = null,
        ?int $to = null,
        ?string $profileType = null,
        ?int $runtimeMode = null,
        ?array $runtime = null,
        ?int $runtimeVersionMode = null,
        ?array $runtimeVersion = null,
        ?int $runtimeArchMode = null,
        ?array $runtimeArch = null,
        ?int $runtimeOsMode = null,
        ?array $runtimeOs = null,
        ?int $probeVersionMode = null,
        ?array $probeVersion = null
    ): RequestInterface {
        // verify the required parameter 'projectId' is set
        if (empty($projectId)) {
            throw new InvalidArgumentException(
                'Missing the required parameter $projectId
                when calling getApplicationFilter'
            );
        }

        if (!preg_match("/[a-z0-9]+/", $projectId)) {
            throw new InvalidArgumentException(
                "invalid value for \"projectId\" when calling ContinuousProfilingApi.getApplicationFilter,
                must conform to the pattern /[a-z0-9]+/."
            );
        }

        // verify the required parameter 'envId' is set
        if (empty($envId)) {
            throw new InvalidArgumentException(
                'Missing the required parameter $envId
                when calling getApplicationFilter'
            );
        }

        if (!preg_match("/.+/", $envId)) {
            throw new InvalidArgumentException(
                "invalid value for \"envId\" when calling ContinuousProfilingApi.getApplicationFilter,
                must conform to the pattern /.+/."
            );
        }

        // verify the required parameter 'app' is set
        if (empty($app)) {
            throw new InvalidArgumentException(
                'Missing the required parameter $app
                when calling getApplicationFilter'
            );
        }

        if (!preg_match("/.+/", $app)) {
            throw new InvalidArgumentException(
                "invalid value for \"app\" when calling ContinuousProfilingApi.getApplicationFilter,
                must conform to the pattern /.+/."
            );
        }

        $resourcePath = '/projects/{projectId}/environments/{envId}/continuous-profiling/app/{app}/filter';
        $formParams = [];
        $queryParams = [];
        $headerParams = [];
        $httpBody = null;
        $multipart = false;

        // query params
        if ($from !== null) {
            if ('form' === 'form' && is_array($from)) {
                foreach ($from as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['from'] = $from instanceof DateTime
                    ? $from->format(DATE_ATOM)
                    : ($from);
            }
        }

        // query params
        if ($to !== null) {
            if ('form' === 'form' && is_array($to)) {
                foreach ($to as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['to'] = $to instanceof DateTime
                    ? $to->format(DATE_ATOM)
                    : ($to);
            }
        }

        // query params
        if ($profileType !== null) {
            if ('form' === 'form' && is_array($profileType)) {
                foreach ($profileType as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['profile_type'] = $profileType instanceof DateTime
                    ? $profileType->format(DATE_ATOM)
                    : ($profileType);
            }
        }

        // query params
        if ($runtimeMode !== null) {
            if ('form' === 'form' && is_array($runtimeMode)) {
                foreach ($runtimeMode as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['runtime_mode'] = $runtimeMode instanceof DateTime
                    ? $runtimeMode->format(DATE_ATOM)
                    : ($runtimeMode);
            }
        }

        // query params
        if ($runtime !== null) {
            if ('form' === 'form' && is_array($runtime)) {
                foreach ($runtime as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['runtime[]'] = $runtime instanceof DateTime
                    ? $runtime->format(DATE_ATOM)
                    : ($runtime);
            }
        }

        // query params
        if ($runtimeVersionMode !== null) {
            if ('form' === 'form' && is_array($runtimeVersionMode)) {
                foreach ($runtimeVersionMode as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['runtime_version_mode'] = $runtimeVersionMode instanceof DateTime
                    ? $runtimeVersionMode->format(DATE_ATOM)
                    : ($runtimeVersionMode);
            }
        }

        // query params
        if ($runtimeVersion !== null) {
            if ('form' === 'form' && is_array($runtimeVersion)) {
                foreach ($runtimeVersion as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['runtime_version[]'] = $runtimeVersion instanceof DateTime
                    ? $runtimeVersion->format(DATE_ATOM)
                    : ($runtimeVersion);
            }
        }

        // query params
        if ($runtimeArchMode !== null) {
            if ('form' === 'form' && is_array($runtimeArchMode)) {
                foreach ($runtimeArchMode as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['runtime_arch_mode'] = $runtimeArchMode instanceof DateTime
                    ? $runtimeArchMode->format(DATE_ATOM)
                    : ($runtimeArchMode);
            }
        }

        // query params
        if ($runtimeArch !== null) {
            if ('form' === 'form' && is_array($runtimeArch)) {
                foreach ($runtimeArch as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['runtime_arch[]'] = $runtimeArch instanceof DateTime
                    ? $runtimeArch->format(DATE_ATOM)
                    : ($runtimeArch);
            }
        }

        // query params
        if ($runtimeOsMode !== null) {
            if ('form' === 'form' && is_array($runtimeOsMode)) {
                foreach ($runtimeOsMode as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['runtime_os_mode'] = $runtimeOsMode instanceof DateTime
                    ? $runtimeOsMode->format(DATE_ATOM)
                    : ($runtimeOsMode);
            }
        }

        // query params
        if ($runtimeOs !== null) {
            if ('form' === 'form' && is_array($runtimeOs)) {
                foreach ($runtimeOs as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['runtime_os[]'] = $runtimeOs instanceof DateTime
                    ? $runtimeOs->format(DATE_ATOM)
                    : ($runtimeOs);
            }
        }

        // query params
        if ($probeVersionMode !== null) {
            if ('form' === 'form' && is_array($probeVersionMode)) {
                foreach ($probeVersionMode as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['probe_version_mode'] = $probeVersionMode instanceof DateTime
                    ? $probeVersionMode->format(DATE_ATOM)
                    : ($probeVersionMode);
            }
        }

        // query params
        if ($probeVersion !== null) {
            if ('form' === 'form' && is_array($probeVersion)) {
                foreach ($probeVersion as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['probe_version[]'] = $probeVersion instanceof DateTime
                    ? $probeVersion->format(DATE_ATOM)
                    : ($probeVersion);
            }
        }

        // path params

        if ($projectId !== null) {
            $resourcePath = str_replace(
                '{' . 'projectId' . '}',
                ObjectSerializer::toPathValue($projectId),
                $resourcePath
            );
        }
        // path params

        if ($envId !== null) {
            $resourcePath = str_replace(
                '{' . 'envId' . '}',
                ObjectSerializer::toPathValue($envId),
                $resourcePath
            );
        }
        // path params

        if ($app !== null) {
            $resourcePath = str_replace(
                '{' . 'app' . '}',
                ObjectSerializer::toPathValue($app),
                $resourcePath
            );
        }

        $headers = $this->headerSelector->selectHeaders(
            ['application/json'],
            '',
            $multipart
        );

        // for model (json/xml)
        if ($formParams !== []) {
            if ($multipart) {
                $multipartContents = [];
                foreach ($formParams as $formParamName => $formParamValue) {
                    $formParamValueItems = is_array($formParamValue) ? $formParamValue : [$formParamValue];
                    foreach ($formParamValueItems as $formParamValueItem) {
                        $multipartContents[] = [
                            'name' => $formParamName,
                            'contents' => $formParamValueItem
                        ];
                    }
                }

                // for HTTP post (form)
                $httpBody = new MultipartStream($multipartContents);
            } elseif ($this->headerSelector->isJsonMime($headers['Content-Type'])) {
                $httpBody = json_encode($formParams);
            } else {
                // for HTTP post (form)
                $httpBody = ObjectSerializer::buildQuery($formParams);
            }
        }

        // this endpoint requires Bearer authentication (access token)
        if ($this->config->getAccessToken() !== null) {
            $headers['Authorization'] = 'Bearer ' . $this->config->getAccessToken();
        }

        $defaultHeaders = [];
        if ($this->config->getUserAgent()) {
            $defaultHeaders['User-Agent'] = $this->config->getUserAgent();
        }
        if ($this->config->getUpsunClientHeader()) {
            $defaultHeaders['X-Upsun-Client'] = $this->config->getUpsunClientHeader();
        }

        $headers = array_merge(
            $defaultHeaders,
            $headerParams,
            $headers
        );

        $operationHost = $this->config->getHost();

        $uri = $this->createUri($operationHost, $resourcePath, $queryParams);

        return $this->createRequest('GET', $uri, $headers, $httpBody);
    }

    /**
     * Get merged profile
     *
     * Returns a merged profile for the given application over the specified time range. The output format is controlled
     * by the `out` query parameter: `pprof` returns a gzipped protobuf with Content-Disposition header, `dot` returns a
     * GraphViz DOT graph, `flamebearerv2` returns JSON flame graph data
     *
     * @param  string $projectId (required)
     * @param  string $envId (required)
     * @param  string $app (required)
     * @param  int|null $from (optional)
     * @param  int|null $to (optional)
     * @param  string|null $profileType (optional)
     * @param  string|null $out (optional)
     * @param  int|null $runtimeMode
     *         Filter mode for runtime parameter. "1" (additive) includes only specified
     *         runtimes. "-1" (subtractive) excludes specified runtimes and includes all
     *         others. When omitted while runtime[] is supplied, defaults to "1" (additive). (optional)
     * @param  string[]|null $runtime
     *         Filter by runtime language. Can be specified multiple times for multiple values
     *         (e.g., ?runtime[]=go&runtime[]=python). Use runtime_mode to control
     *         inclusion/exclusion behavior. (optional)
     * @param  int|null $runtimeVersionMode
     *         Filter mode for runtime_version parameter. "1" (additive) includes only
     *         specified versions. "-1" (subtractive) excludes specified versions and includes
     *         all others. When omitted while runtime_version[] is supplied, defaults to "1"
     *         (additive). (optional)
     * @param  string[]|null $runtimeVersion
     *         Filter by runtime version. Can be specified multiple times for multiple values
     *         (e.g., ?runtime_version[]=1.21&runtime_version[]=1.22). Use runtime_version_mode
     *         to control inclusion/exclusion behavior. (optional)
     * @param  int|null $runtimeArchMode
     *         Filter mode for runtime_arch parameter. "1" (additive) includes only specified
     *         architectures. "-1" (subtractive) excludes specified architectures and includes
     *         all others. When omitted while runtime_arch[] is supplied, defaults to "1"
     *         (additive). (optional)
     * @param  string[]|null $runtimeArch
     *         Filter by runtime architecture. Can be specified multiple times for multiple
     *         values (e.g., ?runtime_arch[]=amd64&runtime_arch[]=arm64). Use runtime_arch_mode
     *         to control inclusion/exclusion behavior. (optional)
     * @param  int|null $runtimeOsMode
     *         Filter mode for runtime_os parameter. "1" (additive) includes only specified
     *         operating systems. "-1" (subtractive) excludes specified operating systems and
     *         includes all others. When omitted while runtime_os[] is supplied, defaults to
     *         "1" (additive). (optional)
     * @param  string[]|null $runtimeOs
     *         Filter by runtime operating system. Can be specified multiple times for multiple
     *         values (e.g., ?runtime_os[]=linux&runtime_os[]=darwin). Use runtime_os_mode to
     *         control inclusion/exclusion behavior. (optional)
     * @param  int|null $probeVersionMode
     *         Filter mode for probe_version parameter. "1" (additive) includes only specified
     *         probe versions. "-1" (subtractive) excludes specified probe versions and
     *         includes all others. When omitted while probe_version[] is supplied, defaults to
     *         "1" (additive). (optional)
     * @param  string[]|null $probeVersion
     *         Filter by probe version. Can be specified multiple times for multiple values
     *         (e.g., ?probe_version[]=2.28.2&probe_version[]=2.28.1). Use probe_version_mode
     *         to control inclusion/exclusion behavior. (optional)
     *
     * @throws ApiException on non-2xx response or if the response body is not in the expected format
     * @throws ClientExceptionInterface
     * @see https://docs.upsun.com/api/#tag/Continuous-Profiling/operation/get_application_merge
     */
    public function getApplicationMerge(
        string $projectId,
        string $envId,
        string $app,
        ?int $from = null,
        ?int $to = null,
        ?string $profileType = null,
        ?string $out = null,
        ?int $runtimeMode = null,
        ?array $runtime = null,
        ?int $runtimeVersionMode = null,
        ?array $runtimeVersion = null,
        ?int $runtimeArchMode = null,
        ?array $runtimeArch = null,
        ?int $runtimeOsMode = null,
        ?array $runtimeOs = null,
        ?int $probeVersionMode = null,
        ?array $probeVersion = null
    ): mixed {
        return $this->getApplicationMergeWithHttpInfo(
            $projectId,
            $envId,
            $app,
            $from,
            $to,
            $profileType,
            $out,
            $runtimeMode,
            $runtime,
            $runtimeVersionMode,
            $runtimeVersion,
            $runtimeArchMode,
            $runtimeArch,
            $runtimeOsMode,
            $runtimeOs,
            $probeVersionMode,
            $probeVersion
        );
    }

    /**
     * Get merged profile with HTTP Info
     *
     * @param  string $projectId (required)
     * @param  string $envId (required)
     * @param  string $app (required)
     * @param  int|null $from (optional)
     * @param  int|null $to (optional)
     * @param  string|null $profileType (optional)
     * @param  string|null $out (optional)
     * @param  int|null $runtimeMode
     *         Filter mode for runtime parameter. "1" (additive) includes only specified
     *         runtimes. "-1" (subtractive) excludes specified runtimes and includes all
     *         others. When omitted while runtime[] is supplied, defaults to "1" (additive). (optional)
     * @param  string[]|null $runtime
     *         Filter by runtime language. Can be specified multiple times for multiple values
     *         (e.g., ?runtime[]=go&runtime[]=python). Use runtime_mode to control
     *         inclusion/exclusion behavior. (optional)
     * @param  int|null $runtimeVersionMode
     *         Filter mode for runtime_version parameter. "1" (additive) includes only
     *         specified versions. "-1" (subtractive) excludes specified versions and includes
     *         all others. When omitted while runtime_version[] is supplied, defaults to "1"
     *         (additive). (optional)
     * @param  string[]|null $runtimeVersion
     *         Filter by runtime version. Can be specified multiple times for multiple values
     *         (e.g., ?runtime_version[]=1.21&runtime_version[]=1.22). Use runtime_version_mode
     *         to control inclusion/exclusion behavior. (optional)
     * @param  int|null $runtimeArchMode
     *         Filter mode for runtime_arch parameter. "1" (additive) includes only specified
     *         architectures. "-1" (subtractive) excludes specified architectures and includes
     *         all others. When omitted while runtime_arch[] is supplied, defaults to "1"
     *         (additive). (optional)
     * @param  string[]|null $runtimeArch
     *         Filter by runtime architecture. Can be specified multiple times for multiple
     *         values (e.g., ?runtime_arch[]=amd64&runtime_arch[]=arm64). Use runtime_arch_mode
     *         to control inclusion/exclusion behavior. (optional)
     * @param  int|null $runtimeOsMode
     *         Filter mode for runtime_os parameter. "1" (additive) includes only specified
     *         operating systems. "-1" (subtractive) excludes specified operating systems and
     *         includes all others. When omitted while runtime_os[] is supplied, defaults to
     *         "1" (additive). (optional)
     * @param  string[]|null $runtimeOs
     *         Filter by runtime operating system. Can be specified multiple times for multiple
     *         values (e.g., ?runtime_os[]=linux&runtime_os[]=darwin). Use runtime_os_mode to
     *         control inclusion/exclusion behavior. (optional)
     * @param  int|null $probeVersionMode
     *         Filter mode for probe_version parameter. "1" (additive) includes only specified
     *         probe versions. "-1" (subtractive) excludes specified probe versions and
     *         includes all others. When omitted while probe_version[] is supplied, defaults to
     *         "1" (additive). (optional)
     * @param  string[]|null $probeVersion
     *         Filter by probe version. Can be specified multiple times for multiple values
     *         (e.g., ?probe_version[]=2.28.2&probe_version[]=2.28.1). Use probe_version_mode
     *         to control inclusion/exclusion behavior. (optional)
     *
     * @throws ApiException on non-2xx response or if the response body is not in the expected format
     * @throws ClientExceptionInterface
    */
    private function getApplicationMergeWithHttpInfo(
        string $projectId,
        string $envId,
        string $app,
        ?int $from = null,
        ?int $to = null,
        ?string $profileType = null,
        ?string $out = null,
        ?int $runtimeMode = null,
        ?array $runtime = null,
        ?int $runtimeVersionMode = null,
        ?array $runtimeVersion = null,
        ?int $runtimeArchMode = null,
        ?array $runtimeArch = null,
        ?int $runtimeOsMode = null,
        ?array $runtimeOs = null,
        ?int $probeVersionMode = null,
        ?array $probeVersion = null
    ): mixed {
        $request = $this->getApplicationMergeRequest(
            $projectId,
            $envId,
            $app,
            $from,
            $to,
            $profileType,
            $out,
            $runtimeMode,
            $runtime,
            $runtimeVersionMode,
            $runtimeVersion,
            $runtimeArchMode,
            $runtimeArch,
            $runtimeOsMode,
            $runtimeOs,
            $probeVersionMode,
            $probeVersion
        );

        try {
            $response = $this->sendAuthenticatedRequest(
                $request->getMethod(),
                (string) $request->getUri(),
                $request->getHeaders(),
                $request->getBody()
            );

            return $this->handleResponseWithDataType(
                '\SplFileObject',
                $request,
                $response
            );
        } catch (Exception $exception) {
            throw new ApiException(
                sprintf(
                    '[%d] Error connecting to the API (%s)',
                    $exception->getCode(),
                    '/projects/{projectId}/environments/{envId}/continuous-profiling/app/{app}/merge'
                ),
                $request,
                $response ?? null,
                $exception
            );
        }
    }

    /**
     * Create request for operation 'getApplicationMerge'
     *
     * @param  string $projectId (required)
     * @param  string $envId (required)
     * @param  string $app (required)
     * @param  int|null $from (optional)
     * @param  int|null $to (optional)
     * @param  string|null $profileType (optional)
     * @param  string|null $out (optional)
     * @param  int|null $runtimeMode
     *         Filter mode for runtime parameter. "1" (additive) includes only specified
     *         runtimes. "-1" (subtractive) excludes specified runtimes and includes all
     *         others. When omitted while runtime[] is supplied, defaults to "1" (additive). (optional)
     * @param  string[]|null $runtime
     *         Filter by runtime language. Can be specified multiple times for multiple values
     *         (e.g., ?runtime[]=go&runtime[]=python). Use runtime_mode to control
     *         inclusion/exclusion behavior. (optional)
     * @param  int|null $runtimeVersionMode
     *         Filter mode for runtime_version parameter. "1" (additive) includes only
     *         specified versions. "-1" (subtractive) excludes specified versions and includes
     *         all others. When omitted while runtime_version[] is supplied, defaults to "1"
     *         (additive). (optional)
     * @param  string[]|null $runtimeVersion
     *         Filter by runtime version. Can be specified multiple times for multiple values
     *         (e.g., ?runtime_version[]=1.21&runtime_version[]=1.22). Use runtime_version_mode
     *         to control inclusion/exclusion behavior. (optional)
     * @param  int|null $runtimeArchMode
     *         Filter mode for runtime_arch parameter. "1" (additive) includes only specified
     *         architectures. "-1" (subtractive) excludes specified architectures and includes
     *         all others. When omitted while runtime_arch[] is supplied, defaults to "1"
     *         (additive). (optional)
     * @param  string[]|null $runtimeArch
     *         Filter by runtime architecture. Can be specified multiple times for multiple
     *         values (e.g., ?runtime_arch[]=amd64&runtime_arch[]=arm64). Use runtime_arch_mode
     *         to control inclusion/exclusion behavior. (optional)
     * @param  int|null $runtimeOsMode
     *         Filter mode for runtime_os parameter. "1" (additive) includes only specified
     *         operating systems. "-1" (subtractive) excludes specified operating systems and
     *         includes all others. When omitted while runtime_os[] is supplied, defaults to
     *         "1" (additive). (optional)
     * @param  string[]|null $runtimeOs
     *         Filter by runtime operating system. Can be specified multiple times for multiple
     *         values (e.g., ?runtime_os[]=linux&runtime_os[]=darwin). Use runtime_os_mode to
     *         control inclusion/exclusion behavior. (optional)
     * @param  int|null $probeVersionMode
     *         Filter mode for probe_version parameter. "1" (additive) includes only specified
     *         probe versions. "-1" (subtractive) excludes specified probe versions and
     *         includes all others. When omitted while probe_version[] is supplied, defaults to
     *         "1" (additive). (optional)
     * @param  string[]|null $probeVersion
     *         Filter by probe version. Can be specified multiple times for multiple values
     *         (e.g., ?probe_version[]=2.28.2&probe_version[]=2.28.1). Use probe_version_mode
     *         to control inclusion/exclusion behavior. (optional)
     *
     * @throws InvalidArgumentException
     */
    private function getApplicationMergeRequest(
        string $projectId,
        string $envId,
        string $app,
        ?int $from = null,
        ?int $to = null,
        ?string $profileType = null,
        ?string $out = null,
        ?int $runtimeMode = null,
        ?array $runtime = null,
        ?int $runtimeVersionMode = null,
        ?array $runtimeVersion = null,
        ?int $runtimeArchMode = null,
        ?array $runtimeArch = null,
        ?int $runtimeOsMode = null,
        ?array $runtimeOs = null,
        ?int $probeVersionMode = null,
        ?array $probeVersion = null
    ): RequestInterface {
        // verify the required parameter 'projectId' is set
        if (empty($projectId)) {
            throw new InvalidArgumentException(
                'Missing the required parameter $projectId
                when calling getApplicationMerge'
            );
        }

        if (!preg_match("/[a-z0-9]+/", $projectId)) {
            throw new InvalidArgumentException(
                "invalid value for \"projectId\" when calling ContinuousProfilingApi.getApplicationMerge,
                must conform to the pattern /[a-z0-9]+/."
            );
        }

        // verify the required parameter 'envId' is set
        if (empty($envId)) {
            throw new InvalidArgumentException(
                'Missing the required parameter $envId
                when calling getApplicationMerge'
            );
        }

        if (!preg_match("/.+/", $envId)) {
            throw new InvalidArgumentException(
                "invalid value for \"envId\" when calling ContinuousProfilingApi.getApplicationMerge,
                must conform to the pattern /.+/."
            );
        }

        // verify the required parameter 'app' is set
        if (empty($app)) {
            throw new InvalidArgumentException(
                'Missing the required parameter $app
                when calling getApplicationMerge'
            );
        }

        if (!preg_match("/.+/", $app)) {
            throw new InvalidArgumentException(
                "invalid value for \"app\" when calling ContinuousProfilingApi.getApplicationMerge,
                must conform to the pattern /.+/."
            );
        }

        $resourcePath = '/projects/{projectId}/environments/{envId}/continuous-profiling/app/{app}/merge';
        $formParams = [];
        $queryParams = [];
        $headerParams = [];
        $httpBody = null;
        $multipart = false;

        // query params
        if ($from !== null) {
            if ('form' === 'form' && is_array($from)) {
                foreach ($from as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['from'] = $from instanceof DateTime
                    ? $from->format(DATE_ATOM)
                    : ($from);
            }
        }

        // query params
        if ($to !== null) {
            if ('form' === 'form' && is_array($to)) {
                foreach ($to as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['to'] = $to instanceof DateTime
                    ? $to->format(DATE_ATOM)
                    : ($to);
            }
        }

        // query params
        if ($profileType !== null) {
            if ('form' === 'form' && is_array($profileType)) {
                foreach ($profileType as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['profile_type'] = $profileType instanceof DateTime
                    ? $profileType->format(DATE_ATOM)
                    : ($profileType);
            }
        }

        // query params
        if ($out !== null) {
            if ('form' === 'form' && is_array($out)) {
                foreach ($out as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['out'] = $out instanceof DateTime
                    ? $out->format(DATE_ATOM)
                    : ($out);
            }
        }

        // query params
        if ($runtimeMode !== null) {
            if ('form' === 'form' && is_array($runtimeMode)) {
                foreach ($runtimeMode as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['runtime_mode'] = $runtimeMode instanceof DateTime
                    ? $runtimeMode->format(DATE_ATOM)
                    : ($runtimeMode);
            }
        }

        // query params
        if ($runtime !== null) {
            if ('form' === 'form' && is_array($runtime)) {
                foreach ($runtime as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['runtime[]'] = $runtime instanceof DateTime
                    ? $runtime->format(DATE_ATOM)
                    : ($runtime);
            }
        }

        // query params
        if ($runtimeVersionMode !== null) {
            if ('form' === 'form' && is_array($runtimeVersionMode)) {
                foreach ($runtimeVersionMode as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['runtime_version_mode'] = $runtimeVersionMode instanceof DateTime
                    ? $runtimeVersionMode->format(DATE_ATOM)
                    : ($runtimeVersionMode);
            }
        }

        // query params
        if ($runtimeVersion !== null) {
            if ('form' === 'form' && is_array($runtimeVersion)) {
                foreach ($runtimeVersion as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['runtime_version[]'] = $runtimeVersion instanceof DateTime
                    ? $runtimeVersion->format(DATE_ATOM)
                    : ($runtimeVersion);
            }
        }

        // query params
        if ($runtimeArchMode !== null) {
            if ('form' === 'form' && is_array($runtimeArchMode)) {
                foreach ($runtimeArchMode as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['runtime_arch_mode'] = $runtimeArchMode instanceof DateTime
                    ? $runtimeArchMode->format(DATE_ATOM)
                    : ($runtimeArchMode);
            }
        }

        // query params
        if ($runtimeArch !== null) {
            if ('form' === 'form' && is_array($runtimeArch)) {
                foreach ($runtimeArch as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['runtime_arch[]'] = $runtimeArch instanceof DateTime
                    ? $runtimeArch->format(DATE_ATOM)
                    : ($runtimeArch);
            }
        }

        // query params
        if ($runtimeOsMode !== null) {
            if ('form' === 'form' && is_array($runtimeOsMode)) {
                foreach ($runtimeOsMode as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['runtime_os_mode'] = $runtimeOsMode instanceof DateTime
                    ? $runtimeOsMode->format(DATE_ATOM)
                    : ($runtimeOsMode);
            }
        }

        // query params
        if ($runtimeOs !== null) {
            if ('form' === 'form' && is_array($runtimeOs)) {
                foreach ($runtimeOs as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['runtime_os[]'] = $runtimeOs instanceof DateTime
                    ? $runtimeOs->format(DATE_ATOM)
                    : ($runtimeOs);
            }
        }

        // query params
        if ($probeVersionMode !== null) {
            if ('form' === 'form' && is_array($probeVersionMode)) {
                foreach ($probeVersionMode as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['probe_version_mode'] = $probeVersionMode instanceof DateTime
                    ? $probeVersionMode->format(DATE_ATOM)
                    : ($probeVersionMode);
            }
        }

        // query params
        if ($probeVersion !== null) {
            if ('form' === 'form' && is_array($probeVersion)) {
                foreach ($probeVersion as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['probe_version[]'] = $probeVersion instanceof DateTime
                    ? $probeVersion->format(DATE_ATOM)
                    : ($probeVersion);
            }
        }

        // path params

        if ($projectId !== null) {
            $resourcePath = str_replace(
                '{' . 'projectId' . '}',
                ObjectSerializer::toPathValue($projectId),
                $resourcePath
            );
        }
        // path params

        if ($envId !== null) {
            $resourcePath = str_replace(
                '{' . 'envId' . '}',
                ObjectSerializer::toPathValue($envId),
                $resourcePath
            );
        }
        // path params

        if ($app !== null) {
            $resourcePath = str_replace(
                '{' . 'app' . '}',
                ObjectSerializer::toPathValue($app),
                $resourcePath
            );
        }

        $headers = $this->headerSelector->selectHeaders(
            ['application/octet-stream', 'text/vnd.graphviz', 'application/json'],
            '',
            $multipart
        );

        // for model (json/xml)
        if ($formParams !== []) {
            if ($multipart) {
                $multipartContents = [];
                foreach ($formParams as $formParamName => $formParamValue) {
                    $formParamValueItems = is_array($formParamValue) ? $formParamValue : [$formParamValue];
                    foreach ($formParamValueItems as $formParamValueItem) {
                        $multipartContents[] = [
                            'name' => $formParamName,
                            'contents' => $formParamValueItem
                        ];
                    }
                }

                // for HTTP post (form)
                $httpBody = new MultipartStream($multipartContents);
            } elseif ($this->headerSelector->isJsonMime($headers['Content-Type'])) {
                $httpBody = json_encode($formParams);
            } else {
                // for HTTP post (form)
                $httpBody = ObjectSerializer::buildQuery($formParams);
            }
        }

        // this endpoint requires Bearer authentication (access token)
        if ($this->config->getAccessToken() !== null) {
            $headers['Authorization'] = 'Bearer ' . $this->config->getAccessToken();
        }

        $defaultHeaders = [];
        if ($this->config->getUserAgent()) {
            $defaultHeaders['User-Agent'] = $this->config->getUserAgent();
        }
        if ($this->config->getUpsunClientHeader()) {
            $defaultHeaders['X-Upsun-Client'] = $this->config->getUpsunClientHeader();
        }

        $headers = array_merge(
            $defaultHeaders,
            $headerParams,
            $headers
        );

        $operationHost = $this->config->getHost();

        $uri = $this->createUri($operationHost, $resourcePath, $queryParams);

        return $this->createRequest('GET', $uri, $headers, $httpBody);
    }

    /**
     * Get application timeline
     *
     * Returns timeline data points for the given application and profile type, scoped to the authenticated agent.
     * Points are aggregated at a computed grain (60s to 1h) based on the time range. The response includes the bounded
     * time range, aggregation metadata, and the agent retention period
     *
     * @param  string $projectId (required)
     * @param  string $envId (required)
     * @param  string $app (required)
     * @param  int|null $from (optional)
     * @param  int|null $to (optional)
     * @param  string|null $profileType (optional)
     * @param  int|null $runtimeMode
     *         Filter mode for runtime parameter. "1" (additive) includes only specified
     *         runtimes. "-1" (subtractive) excludes specified runtimes and includes all
     *         others. When omitted while runtime[] is supplied, defaults to "1" (additive). (optional)
     * @param  string[]|null $runtime
     *         Filter by runtime language. Can be specified multiple times for multiple values
     *         (e.g., ?runtime[]=go&runtime[]=python). Use runtime_mode to control
     *         inclusion/exclusion behavior. (optional)
     * @param  int|null $runtimeVersionMode
     *         Filter mode for runtime_version parameter. "1" (additive) includes only
     *         specified versions. "-1" (subtractive) excludes specified versions and includes
     *         all others. When omitted while runtime_version[] is supplied, defaults to "1"
     *         (additive). (optional)
     * @param  string[]|null $runtimeVersion
     *         Filter by runtime version. Can be specified multiple times for multiple values
     *         (e.g., ?runtime_version[]=1.21&runtime_version[]=1.22). Use runtime_version_mode
     *         to control inclusion/exclusion behavior. (optional)
     * @param  int|null $runtimeArchMode
     *         Filter mode for runtime_arch parameter. "1" (additive) includes only specified
     *         architectures. "-1" (subtractive) excludes specified architectures and includes
     *         all others. When omitted while runtime_arch[] is supplied, defaults to "1"
     *         (additive). (optional)
     * @param  string[]|null $runtimeArch
     *         Filter by runtime architecture. Can be specified multiple times for multiple
     *         values (e.g., ?runtime_arch[]=amd64&runtime_arch[]=arm64). Use runtime_arch_mode
     *         to control inclusion/exclusion behavior. (optional)
     * @param  int|null $runtimeOsMode
     *         Filter mode for runtime_os parameter. "1" (additive) includes only specified
     *         operating systems. "-1" (subtractive) excludes specified operating systems and
     *         includes all others. When omitted while runtime_os[] is supplied, defaults to
     *         "1" (additive). (optional)
     * @param  string[]|null $runtimeOs
     *         Filter by runtime operating system. Can be specified multiple times for multiple
     *         values (e.g., ?runtime_os[]=linux&runtime_os[]=darwin). Use runtime_os_mode to
     *         control inclusion/exclusion behavior. (optional)
     * @param  int|null $probeVersionMode
     *         Filter mode for probe_version parameter. "1" (additive) includes only specified
     *         probe versions. "-1" (subtractive) excludes specified probe versions and
     *         includes all others. When omitted while probe_version[] is supplied, defaults to
     *         "1" (additive). (optional)
     * @param  string[]|null $probeVersion
     *         Filter by probe version. Can be specified multiple times for multiple values
     *         (e.g., ?probe_version[]=2.28.2&probe_version[]=2.28.1). Use probe_version_mode
     *         to control inclusion/exclusion behavior. (optional)
     *
     * @throws ApiException on non-2xx response or if the response body is not in the expected format
     * @throws ClientExceptionInterface
     * @see https://docs.upsun.com/api/#tag/Continuous-Profiling/operation/get_application_timeline
     */
    public function getApplicationTimeline(
        string $projectId,
        string $envId,
        string $app,
        ?int $from = null,
        ?int $to = null,
        ?string $profileType = null,
        ?int $runtimeMode = null,
        ?array $runtime = null,
        ?int $runtimeVersionMode = null,
        ?array $runtimeVersion = null,
        ?int $runtimeArchMode = null,
        ?array $runtimeArch = null,
        ?int $runtimeOsMode = null,
        ?array $runtimeOs = null,
        ?int $probeVersionMode = null,
        ?array $probeVersion = null
    ): mixed {
        return $this->getApplicationTimelineWithHttpInfo(
            $projectId,
            $envId,
            $app,
            $from,
            $to,
            $profileType,
            $runtimeMode,
            $runtime,
            $runtimeVersionMode,
            $runtimeVersion,
            $runtimeArchMode,
            $runtimeArch,
            $runtimeOsMode,
            $runtimeOs,
            $probeVersionMode,
            $probeVersion
        );
    }

    /**
     * Get application timeline with HTTP Info
     *
     * @param  string $projectId (required)
     * @param  string $envId (required)
     * @param  string $app (required)
     * @param  int|null $from (optional)
     * @param  int|null $to (optional)
     * @param  string|null $profileType (optional)
     * @param  int|null $runtimeMode
     *         Filter mode for runtime parameter. "1" (additive) includes only specified
     *         runtimes. "-1" (subtractive) excludes specified runtimes and includes all
     *         others. When omitted while runtime[] is supplied, defaults to "1" (additive). (optional)
     * @param  string[]|null $runtime
     *         Filter by runtime language. Can be specified multiple times for multiple values
     *         (e.g., ?runtime[]=go&runtime[]=python). Use runtime_mode to control
     *         inclusion/exclusion behavior. (optional)
     * @param  int|null $runtimeVersionMode
     *         Filter mode for runtime_version parameter. "1" (additive) includes only
     *         specified versions. "-1" (subtractive) excludes specified versions and includes
     *         all others. When omitted while runtime_version[] is supplied, defaults to "1"
     *         (additive). (optional)
     * @param  string[]|null $runtimeVersion
     *         Filter by runtime version. Can be specified multiple times for multiple values
     *         (e.g., ?runtime_version[]=1.21&runtime_version[]=1.22). Use runtime_version_mode
     *         to control inclusion/exclusion behavior. (optional)
     * @param  int|null $runtimeArchMode
     *         Filter mode for runtime_arch parameter. "1" (additive) includes only specified
     *         architectures. "-1" (subtractive) excludes specified architectures and includes
     *         all others. When omitted while runtime_arch[] is supplied, defaults to "1"
     *         (additive). (optional)
     * @param  string[]|null $runtimeArch
     *         Filter by runtime architecture. Can be specified multiple times for multiple
     *         values (e.g., ?runtime_arch[]=amd64&runtime_arch[]=arm64). Use runtime_arch_mode
     *         to control inclusion/exclusion behavior. (optional)
     * @param  int|null $runtimeOsMode
     *         Filter mode for runtime_os parameter. "1" (additive) includes only specified
     *         operating systems. "-1" (subtractive) excludes specified operating systems and
     *         includes all others. When omitted while runtime_os[] is supplied, defaults to
     *         "1" (additive). (optional)
     * @param  string[]|null $runtimeOs
     *         Filter by runtime operating system. Can be specified multiple times for multiple
     *         values (e.g., ?runtime_os[]=linux&runtime_os[]=darwin). Use runtime_os_mode to
     *         control inclusion/exclusion behavior. (optional)
     * @param  int|null $probeVersionMode
     *         Filter mode for probe_version parameter. "1" (additive) includes only specified
     *         probe versions. "-1" (subtractive) excludes specified probe versions and
     *         includes all others. When omitted while probe_version[] is supplied, defaults to
     *         "1" (additive). (optional)
     * @param  string[]|null $probeVersion
     *         Filter by probe version. Can be specified multiple times for multiple values
     *         (e.g., ?probe_version[]=2.28.2&probe_version[]=2.28.1). Use probe_version_mode
     *         to control inclusion/exclusion behavior. (optional)
     *
     * @throws ApiException on non-2xx response or if the response body is not in the expected format
     * @throws ClientExceptionInterface
    */
    private function getApplicationTimelineWithHttpInfo(
        string $projectId,
        string $envId,
        string $app,
        ?int $from = null,
        ?int $to = null,
        ?string $profileType = null,
        ?int $runtimeMode = null,
        ?array $runtime = null,
        ?int $runtimeVersionMode = null,
        ?array $runtimeVersion = null,
        ?int $runtimeArchMode = null,
        ?array $runtimeArch = null,
        ?int $runtimeOsMode = null,
        ?array $runtimeOs = null,
        ?int $probeVersionMode = null,
        ?array $probeVersion = null
    ): mixed {
        $request = $this->getApplicationTimelineRequest(
            $projectId,
            $envId,
            $app,
            $from,
            $to,
            $profileType,
            $runtimeMode,
            $runtime,
            $runtimeVersionMode,
            $runtimeVersion,
            $runtimeArchMode,
            $runtimeArch,
            $runtimeOsMode,
            $runtimeOs,
            $probeVersionMode,
            $probeVersion
        );

        try {
            $response = $this->sendAuthenticatedRequest(
                $request->getMethod(),
                (string) $request->getUri(),
                $request->getHeaders(),
                $request->getBody()
            );

            return $this->handleResponseWithDataType(
                '\Upsun\Model\GetApplicationTimeline200Response',
                $request,
                $response
            );
        } catch (Exception $exception) {
            throw new ApiException(
                sprintf(
                    '[%d] Error connecting to the API (%s)',
                    $exception->getCode(),
                    '/projects/{projectId}/environments/{envId}/continuous-profiling/app/{app}'
                ),
                $request,
                $response ?? null,
                $exception
            );
        }
    }

    /**
     * Create request for operation 'getApplicationTimeline'
     *
     * @param  string $projectId (required)
     * @param  string $envId (required)
     * @param  string $app (required)
     * @param  int|null $from (optional)
     * @param  int|null $to (optional)
     * @param  string|null $profileType (optional)
     * @param  int|null $runtimeMode
     *         Filter mode for runtime parameter. "1" (additive) includes only specified
     *         runtimes. "-1" (subtractive) excludes specified runtimes and includes all
     *         others. When omitted while runtime[] is supplied, defaults to "1" (additive). (optional)
     * @param  string[]|null $runtime
     *         Filter by runtime language. Can be specified multiple times for multiple values
     *         (e.g., ?runtime[]=go&runtime[]=python). Use runtime_mode to control
     *         inclusion/exclusion behavior. (optional)
     * @param  int|null $runtimeVersionMode
     *         Filter mode for runtime_version parameter. "1" (additive) includes only
     *         specified versions. "-1" (subtractive) excludes specified versions and includes
     *         all others. When omitted while runtime_version[] is supplied, defaults to "1"
     *         (additive). (optional)
     * @param  string[]|null $runtimeVersion
     *         Filter by runtime version. Can be specified multiple times for multiple values
     *         (e.g., ?runtime_version[]=1.21&runtime_version[]=1.22). Use runtime_version_mode
     *         to control inclusion/exclusion behavior. (optional)
     * @param  int|null $runtimeArchMode
     *         Filter mode for runtime_arch parameter. "1" (additive) includes only specified
     *         architectures. "-1" (subtractive) excludes specified architectures and includes
     *         all others. When omitted while runtime_arch[] is supplied, defaults to "1"
     *         (additive). (optional)
     * @param  string[]|null $runtimeArch
     *         Filter by runtime architecture. Can be specified multiple times for multiple
     *         values (e.g., ?runtime_arch[]=amd64&runtime_arch[]=arm64). Use runtime_arch_mode
     *         to control inclusion/exclusion behavior. (optional)
     * @param  int|null $runtimeOsMode
     *         Filter mode for runtime_os parameter. "1" (additive) includes only specified
     *         operating systems. "-1" (subtractive) excludes specified operating systems and
     *         includes all others. When omitted while runtime_os[] is supplied, defaults to
     *         "1" (additive). (optional)
     * @param  string[]|null $runtimeOs
     *         Filter by runtime operating system. Can be specified multiple times for multiple
     *         values (e.g., ?runtime_os[]=linux&runtime_os[]=darwin). Use runtime_os_mode to
     *         control inclusion/exclusion behavior. (optional)
     * @param  int|null $probeVersionMode
     *         Filter mode for probe_version parameter. "1" (additive) includes only specified
     *         probe versions. "-1" (subtractive) excludes specified probe versions and
     *         includes all others. When omitted while probe_version[] is supplied, defaults to
     *         "1" (additive). (optional)
     * @param  string[]|null $probeVersion
     *         Filter by probe version. Can be specified multiple times for multiple values
     *         (e.g., ?probe_version[]=2.28.2&probe_version[]=2.28.1). Use probe_version_mode
     *         to control inclusion/exclusion behavior. (optional)
     *
     * @throws InvalidArgumentException
     */
    private function getApplicationTimelineRequest(
        string $projectId,
        string $envId,
        string $app,
        ?int $from = null,
        ?int $to = null,
        ?string $profileType = null,
        ?int $runtimeMode = null,
        ?array $runtime = null,
        ?int $runtimeVersionMode = null,
        ?array $runtimeVersion = null,
        ?int $runtimeArchMode = null,
        ?array $runtimeArch = null,
        ?int $runtimeOsMode = null,
        ?array $runtimeOs = null,
        ?int $probeVersionMode = null,
        ?array $probeVersion = null
    ): RequestInterface {
        // verify the required parameter 'projectId' is set
        if (empty($projectId)) {
            throw new InvalidArgumentException(
                'Missing the required parameter $projectId
                when calling getApplicationTimeline'
            );
        }

        if (!preg_match("/[a-z0-9]+/", $projectId)) {
            throw new InvalidArgumentException(
                "invalid value for \"projectId\" when calling ContinuousProfilingApi.getApplicationTimeline,
                must conform to the pattern /[a-z0-9]+/."
            );
        }

        // verify the required parameter 'envId' is set
        if (empty($envId)) {
            throw new InvalidArgumentException(
                'Missing the required parameter $envId
                when calling getApplicationTimeline'
            );
        }

        if (!preg_match("/.+/", $envId)) {
            throw new InvalidArgumentException(
                "invalid value for \"envId\" when calling ContinuousProfilingApi.getApplicationTimeline,
                must conform to the pattern /.+/."
            );
        }

        // verify the required parameter 'app' is set
        if (empty($app)) {
            throw new InvalidArgumentException(
                'Missing the required parameter $app
                when calling getApplicationTimeline'
            );
        }

        if (!preg_match("/.+/", $app)) {
            throw new InvalidArgumentException(
                "invalid value for \"app\" when calling ContinuousProfilingApi.getApplicationTimeline,
                must conform to the pattern /.+/."
            );
        }

        $resourcePath = '/projects/{projectId}/environments/{envId}/continuous-profiling/app/{app}';
        $formParams = [];
        $queryParams = [];
        $headerParams = [];
        $httpBody = null;
        $multipart = false;

        // query params
        if ($from !== null) {
            if ('form' === 'form' && is_array($from)) {
                foreach ($from as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['from'] = $from instanceof DateTime
                    ? $from->format(DATE_ATOM)
                    : ($from);
            }
        }

        // query params
        if ($to !== null) {
            if ('form' === 'form' && is_array($to)) {
                foreach ($to as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['to'] = $to instanceof DateTime
                    ? $to->format(DATE_ATOM)
                    : ($to);
            }
        }

        // query params
        if ($profileType !== null) {
            if ('form' === 'form' && is_array($profileType)) {
                foreach ($profileType as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['profile_type'] = $profileType instanceof DateTime
                    ? $profileType->format(DATE_ATOM)
                    : ($profileType);
            }
        }

        // query params
        if ($runtimeMode !== null) {
            if ('form' === 'form' && is_array($runtimeMode)) {
                foreach ($runtimeMode as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['runtime_mode'] = $runtimeMode instanceof DateTime
                    ? $runtimeMode->format(DATE_ATOM)
                    : ($runtimeMode);
            }
        }

        // query params
        if ($runtime !== null) {
            if ('form' === 'form' && is_array($runtime)) {
                foreach ($runtime as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['runtime[]'] = $runtime instanceof DateTime
                    ? $runtime->format(DATE_ATOM)
                    : ($runtime);
            }
        }

        // query params
        if ($runtimeVersionMode !== null) {
            if ('form' === 'form' && is_array($runtimeVersionMode)) {
                foreach ($runtimeVersionMode as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['runtime_version_mode'] = $runtimeVersionMode instanceof DateTime
                    ? $runtimeVersionMode->format(DATE_ATOM)
                    : ($runtimeVersionMode);
            }
        }

        // query params
        if ($runtimeVersion !== null) {
            if ('form' === 'form' && is_array($runtimeVersion)) {
                foreach ($runtimeVersion as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['runtime_version[]'] = $runtimeVersion instanceof DateTime
                    ? $runtimeVersion->format(DATE_ATOM)
                    : ($runtimeVersion);
            }
        }

        // query params
        if ($runtimeArchMode !== null) {
            if ('form' === 'form' && is_array($runtimeArchMode)) {
                foreach ($runtimeArchMode as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['runtime_arch_mode'] = $runtimeArchMode instanceof DateTime
                    ? $runtimeArchMode->format(DATE_ATOM)
                    : ($runtimeArchMode);
            }
        }

        // query params
        if ($runtimeArch !== null) {
            if ('form' === 'form' && is_array($runtimeArch)) {
                foreach ($runtimeArch as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['runtime_arch[]'] = $runtimeArch instanceof DateTime
                    ? $runtimeArch->format(DATE_ATOM)
                    : ($runtimeArch);
            }
        }

        // query params
        if ($runtimeOsMode !== null) {
            if ('form' === 'form' && is_array($runtimeOsMode)) {
                foreach ($runtimeOsMode as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['runtime_os_mode'] = $runtimeOsMode instanceof DateTime
                    ? $runtimeOsMode->format(DATE_ATOM)
                    : ($runtimeOsMode);
            }
        }

        // query params
        if ($runtimeOs !== null) {
            if ('form' === 'form' && is_array($runtimeOs)) {
                foreach ($runtimeOs as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['runtime_os[]'] = $runtimeOs instanceof DateTime
                    ? $runtimeOs->format(DATE_ATOM)
                    : ($runtimeOs);
            }
        }

        // query params
        if ($probeVersionMode !== null) {
            if ('form' === 'form' && is_array($probeVersionMode)) {
                foreach ($probeVersionMode as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['probe_version_mode'] = $probeVersionMode instanceof DateTime
                    ? $probeVersionMode->format(DATE_ATOM)
                    : ($probeVersionMode);
            }
        }

        // query params
        if ($probeVersion !== null) {
            if ('form' === 'form' && is_array($probeVersion)) {
                foreach ($probeVersion as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['probe_version[]'] = $probeVersion instanceof DateTime
                    ? $probeVersion->format(DATE_ATOM)
                    : ($probeVersion);
            }
        }

        // path params

        if ($projectId !== null) {
            $resourcePath = str_replace(
                '{' . 'projectId' . '}',
                ObjectSerializer::toPathValue($projectId),
                $resourcePath
            );
        }
        // path params

        if ($envId !== null) {
            $resourcePath = str_replace(
                '{' . 'envId' . '}',
                ObjectSerializer::toPathValue($envId),
                $resourcePath
            );
        }
        // path params

        if ($app !== null) {
            $resourcePath = str_replace(
                '{' . 'app' . '}',
                ObjectSerializer::toPathValue($app),
                $resourcePath
            );
        }

        $headers = $this->headerSelector->selectHeaders(
            ['application/json'],
            '',
            $multipart
        );

        // for model (json/xml)
        if ($formParams !== []) {
            if ($multipart) {
                $multipartContents = [];
                foreach ($formParams as $formParamName => $formParamValue) {
                    $formParamValueItems = is_array($formParamValue) ? $formParamValue : [$formParamValue];
                    foreach ($formParamValueItems as $formParamValueItem) {
                        $multipartContents[] = [
                            'name' => $formParamName,
                            'contents' => $formParamValueItem
                        ];
                    }
                }

                // for HTTP post (form)
                $httpBody = new MultipartStream($multipartContents);
            } elseif ($this->headerSelector->isJsonMime($headers['Content-Type'])) {
                $httpBody = json_encode($formParams);
            } else {
                // for HTTP post (form)
                $httpBody = ObjectSerializer::buildQuery($formParams);
            }
        }

        // this endpoint requires Bearer authentication (access token)
        if ($this->config->getAccessToken() !== null) {
            $headers['Authorization'] = 'Bearer ' . $this->config->getAccessToken();
        }

        $defaultHeaders = [];
        if ($this->config->getUserAgent()) {
            $defaultHeaders['User-Agent'] = $this->config->getUserAgent();
        }
        if ($this->config->getUpsunClientHeader()) {
            $defaultHeaders['X-Upsun-Client'] = $this->config->getUpsunClientHeader();
        }

        $headers = array_merge(
            $defaultHeaders,
            $headerParams,
            $headers
        );

        $operationHost = $this->config->getHost();

        $uri = $this->createUri($operationHost, $resourcePath, $queryParams);

        return $this->createRequest('GET', $uri, $headers, $httpBody);
    }

    /**
     * List profiled applications
     *
     * Returns all applications reporting continuous profiling data for the authenticated agent, scoped to the given
     * time range. Each application includes its available profile types (with unit and aggregation metadata) and
     * detected languages
     *
     * @param  string $projectId (required)
     * @param  string $envId (required)
     * @param  int|null $from (optional)
     * @param  int|null $to (optional)
     *
     * @throws ApiException on non-2xx response or if the response body is not in the expected format
     * @throws ClientExceptionInterface
     * @see https://docs.upsun.com/api/#tag/Continuous-Profiling/operation/list_applications
     */
    public function listApplications(
        string $projectId,
        string $envId,
        ?int $from = null,
        ?int $to = null
    ): mixed {
        return $this->listApplicationsWithHttpInfo(
            $projectId,
            $envId,
            $from,
            $to
        );
    }

    /**
     * List profiled applications with HTTP Info
     *
     * @param  string $projectId (required)
     * @param  string $envId (required)
     * @param  int|null $from (optional)
     * @param  int|null $to (optional)
     *
     * @throws ApiException on non-2xx response or if the response body is not in the expected format
     * @throws ClientExceptionInterface
    */
    private function listApplicationsWithHttpInfo(
        string $projectId,
        string $envId,
        ?int $from = null,
        ?int $to = null
    ): mixed {
        $request = $this->listApplicationsRequest(
            $projectId,
            $envId,
            $from,
            $to
        );

        try {
            $response = $this->sendAuthenticatedRequest(
                $request->getMethod(),
                (string) $request->getUri(),
                $request->getHeaders(),
                $request->getBody()
            );

            return $this->handleResponseWithDataType(
                '\Upsun\Model\ListApplications200Response',
                $request,
                $response
            );
        } catch (Exception $exception) {
            throw new ApiException(
                sprintf(
                    '[%d] Error connecting to the API (%s)',
                    $exception->getCode(),
                    '/projects/{projectId}/environments/{envId}/continuous-profiling'
                ),
                $request,
                $response ?? null,
                $exception
            );
        }
    }

    /**
     * Create request for operation 'listApplications'
     *
     * @param  string $projectId (required)
     * @param  string $envId (required)
     * @param  int|null $from (optional)
     * @param  int|null $to (optional)
     *
     * @throws InvalidArgumentException
     */
    private function listApplicationsRequest(
        string $projectId,
        string $envId,
        ?int $from = null,
        ?int $to = null
    ): RequestInterface {
        // verify the required parameter 'projectId' is set
        if (empty($projectId)) {
            throw new InvalidArgumentException(
                'Missing the required parameter $projectId
                when calling listApplications'
            );
        }

        if (!preg_match("/[a-z0-9]+/", $projectId)) {
            throw new InvalidArgumentException(
                "invalid value for \"projectId\" when calling ContinuousProfilingApi.listApplications,
                must conform to the pattern /[a-z0-9]+/."
            );
        }

        // verify the required parameter 'envId' is set
        if (empty($envId)) {
            throw new InvalidArgumentException(
                'Missing the required parameter $envId
                when calling listApplications'
            );
        }

        if (!preg_match("/.+/", $envId)) {
            throw new InvalidArgumentException(
                "invalid value for \"envId\" when calling ContinuousProfilingApi.listApplications,
                must conform to the pattern /.+/."
            );
        }

        $resourcePath = '/projects/{projectId}/environments/{envId}/continuous-profiling';
        $formParams = [];
        $queryParams = [];
        $headerParams = [];
        $httpBody = null;
        $multipart = false;

        // query params
        if ($from !== null) {
            if ('form' === 'form' && is_array($from)) {
                foreach ($from as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['from'] = $from instanceof DateTime
                    ? $from->format(DATE_ATOM)
                    : ($from);
            }
        }

        // query params
        if ($to !== null) {
            if ('form' === 'form' && is_array($to)) {
                foreach ($to as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['to'] = $to instanceof DateTime
                    ? $to->format(DATE_ATOM)
                    : ($to);
            }
        }

        // path params

        if ($projectId !== null) {
            $resourcePath = str_replace(
                '{' . 'projectId' . '}',
                ObjectSerializer::toPathValue($projectId),
                $resourcePath
            );
        }
        // path params

        if ($envId !== null) {
            $resourcePath = str_replace(
                '{' . 'envId' . '}',
                ObjectSerializer::toPathValue($envId),
                $resourcePath
            );
        }

        $headers = $this->headerSelector->selectHeaders(
            ['application/json'],
            '',
            $multipart
        );

        // for model (json/xml)
        if ($formParams !== []) {
            if ($multipart) {
                $multipartContents = [];
                foreach ($formParams as $formParamName => $formParamValue) {
                    $formParamValueItems = is_array($formParamValue) ? $formParamValue : [$formParamValue];
                    foreach ($formParamValueItems as $formParamValueItem) {
                        $multipartContents[] = [
                            'name' => $formParamName,
                            'contents' => $formParamValueItem
                        ];
                    }
                }

                // for HTTP post (form)
                $httpBody = new MultipartStream($multipartContents);
            } elseif ($this->headerSelector->isJsonMime($headers['Content-Type'])) {
                $httpBody = json_encode($formParams);
            } else {
                // for HTTP post (form)
                $httpBody = ObjectSerializer::buildQuery($formParams);
            }
        }

        // this endpoint requires Bearer authentication (access token)
        if ($this->config->getAccessToken() !== null) {
            $headers['Authorization'] = 'Bearer ' . $this->config->getAccessToken();
        }

        $defaultHeaders = [];
        if ($this->config->getUserAgent()) {
            $defaultHeaders['User-Agent'] = $this->config->getUserAgent();
        }
        if ($this->config->getUpsunClientHeader()) {
            $defaultHeaders['X-Upsun-Client'] = $this->config->getUpsunClientHeader();
        }

        $headers = array_merge(
            $defaultHeaders,
            $headerParams,
            $headers
        );

        $operationHost = $this->config->getHost();

        $uri = $this->createUri($operationHost, $resourcePath, $queryParams);

        return $this->createRequest('GET', $uri, $headers, $httpBody);
    }
}
