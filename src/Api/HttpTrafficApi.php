<?php

namespace Upsun\Api;

use DateTime;
use Exception;
use GuzzleHttp\Psr7\MultipartStream;
use InvalidArgumentException;
use Psr\Http\Client\ClientExceptionInterface;
use Psr\Http\Client\ClientInterface;
use Psr\Http\Message\RequestFactoryInterface;
use Psr\Http\Message\RequestInterface;
use Psr\Http\Message\StreamFactoryInterface;
use Upsun\Api\Serializer\ObjectSerializer;
use Upsun\Core\OAuthProvider;

/**
 * Low level HttpTrafficApi (auto-generated)
 *
 * @license   Apache-2.0
 * @see       https://docs.upsun.com
 * @generated This file was generated by OpenAPI Generator. Do not edit manually.
 */
final class HttpTrafficApi extends AbstractApi
{
    private readonly ApiHeaderSelector $headerSelector;

    private APIConfiguration $config;

    public function __construct(
        OAuthProvider $oauthProvider,
        ?ClientInterface $httpClient = null,
        ?RequestFactoryInterface $requestFactory = null,
        ?APIConfiguration $config = null,
        ?StreamFactoryInterface $streamFactory = null,
        ?ApiHeaderSelector $selector = null,
    ) {
        parent::__construct(
            $oauthProvider,
            $httpClient,
            $requestFactory,
            AbstractApi::BASE_PATH,
            $streamFactory
        );

        $this->config = $config ?? (new APIConfiguration())->setHost(AbstractApi::BASE_PATH);

        $this->headerSelector = $selector ?? new ApiHeaderSelector();
    }

    /**
     * Get HTTP traffic timeline by IP
     *
     * Returns HTTP traffic analytics grouped by IP address with IPs ranked by impact (response time × request count).
     * Use limit parameter to control total items returned (default: 200, max: 300) and top_hits_count to control how
     * many items are flagged as top hits and tracked in the timeline (default: 10, max: 15). Items with index <
     * top_hits_count are flagged with top_hit: true and tracked in top_hits_timeline; the remainder (up to limit) are
     * returned with top_hit: false.
     *
     * @param  string $projectId (required)
     * @param  string $environmentId (required)
     * @param  int $from (required)
     * @param  int $to (required)
     * @param  int|null $limit (optional)
     * @param  int|null $topHitsCount (optional)
     * @param  string[]|null $applications (optional)
     * @param  string|null $applicationsMode (optional)
     * @param  string[]|null $methods (optional)
     * @param  string|null $methodsMode (optional)
     * @param  string[]|null $domains (optional)
     * @param  string|null $domainsMode (optional)
     * @param  string[]|null $codeSlots (optional)
     * @param  string|null $codeSlotsMode (optional)
     * @param  string[]|null $codes (optional)
     * @param  string|null $codesMode (optional)
     * @param  string[]|null $requestDurationSlots (optional)
     * @param  string|null $requestDurationSlotsMode (optional)
     *
     * @throws ApiException on non-2xx response or if the response body is not in the expected format
     * @throws ClientExceptionInterface
     * @see https://docs.upsun.com/api/#tag/Http-Traffic/operation/http_metrics_timeline_ips
     */
    public function httpMetricsTimelineIps(
        string $projectId,
        string $environmentId,
        int $from,
        int $to,
        ?int $limit = null,
        ?int $topHitsCount = null,
        ?array $applications = null,
        ?string $applicationsMode = null,
        ?array $methods = null,
        ?string $methodsMode = null,
        ?array $domains = null,
        ?string $domainsMode = null,
        ?array $codeSlots = null,
        ?string $codeSlotsMode = null,
        ?array $codes = null,
        ?string $codesMode = null,
        ?array $requestDurationSlots = null,
        ?string $requestDurationSlotsMode = null
    ): mixed {
        return $this->httpMetricsTimelineIpsWithHttpInfo(
            $projectId,
            $environmentId,
            $from,
            $to,
            $limit,
            $topHitsCount,
            $applications,
            $applicationsMode,
            $methods,
            $methodsMode,
            $domains,
            $domainsMode,
            $codeSlots,
            $codeSlotsMode,
            $codes,
            $codesMode,
            $requestDurationSlots,
            $requestDurationSlotsMode
        );
    }

    /**
     * Get HTTP traffic timeline by IP with HTTP Info
     *
     * @param  string $projectId (required)
     * @param  string $environmentId (required)
     * @param  int $from (required)
     * @param  int $to (required)
     * @param  int|null $limit (optional)
     * @param  int|null $topHitsCount (optional)
     * @param  string[]|null $applications (optional)
     * @param  string|null $applicationsMode (optional)
     * @param  string[]|null $methods (optional)
     * @param  string|null $methodsMode (optional)
     * @param  string[]|null $domains (optional)
     * @param  string|null $domainsMode (optional)
     * @param  string[]|null $codeSlots (optional)
     * @param  string|null $codeSlotsMode (optional)
     * @param  string[]|null $codes (optional)
     * @param  string|null $codesMode (optional)
     * @param  string[]|null $requestDurationSlots (optional)
     * @param  string|null $requestDurationSlotsMode (optional)
     *
     * @throws ApiException on non-2xx response or if the response body is not in the expected format
     * @throws ClientExceptionInterface
    */
    private function httpMetricsTimelineIpsWithHttpInfo(
        string $projectId,
        string $environmentId,
        int $from,
        int $to,
        ?int $limit = null,
        ?int $topHitsCount = null,
        ?array $applications = null,
        ?string $applicationsMode = null,
        ?array $methods = null,
        ?string $methodsMode = null,
        ?array $domains = null,
        ?string $domainsMode = null,
        ?array $codeSlots = null,
        ?string $codeSlotsMode = null,
        ?array $codes = null,
        ?string $codesMode = null,
        ?array $requestDurationSlots = null,
        ?string $requestDurationSlotsMode = null
    ): mixed {
        $request = $this->httpMetricsTimelineIpsRequest(
            $projectId,
            $environmentId,
            $from,
            $to,
            $limit,
            $topHitsCount,
            $applications,
            $applicationsMode,
            $methods,
            $methodsMode,
            $domains,
            $domainsMode,
            $codeSlots,
            $codeSlotsMode,
            $codes,
            $codesMode,
            $requestDurationSlots,
            $requestDurationSlotsMode
        );

        try {
            $response = $this->sendAuthenticatedRequest(
                $request->getMethod(),
                (string) $request->getUri(),
                $request->getHeaders(),
                $request->getBody()
            );

            return $this->handleResponseWithDataType(
                '\Upsun\Model\HttpMetricsTimelineIps200Response',
                $request,
                $response
            );
        } catch (Exception $exception) {
            throw new ApiException(
                sprintf(
                    '[%d] Error connecting to the API (%s)',
                    $exception->getCode(),
                    '/projects/{projectId}/environments/{environmentId}/observability/http/breakdown/ips'
                ),
                $request,
                $response ?? null,
                $exception
            );
        }
    }

    /**
     * Create request for operation 'httpMetricsTimelineIps'
     *
     * @param  string $projectId (required)
     * @param  string $environmentId (required)
     * @param  int $from (required)
     * @param  int $to (required)
     * @param  int|null $limit (optional)
     * @param  int|null $topHitsCount (optional)
     * @param  string[]|null $applications (optional)
     * @param  string|null $applicationsMode (optional)
     * @param  string[]|null $methods (optional)
     * @param  string|null $methodsMode (optional)
     * @param  string[]|null $domains (optional)
     * @param  string|null $domainsMode (optional)
     * @param  string[]|null $codeSlots (optional)
     * @param  string|null $codeSlotsMode (optional)
     * @param  string[]|null $codes (optional)
     * @param  string|null $codesMode (optional)
     * @param  string[]|null $requestDurationSlots (optional)
     * @param  string|null $requestDurationSlotsMode (optional)
     *
     * @throws InvalidArgumentException
     */
    private function httpMetricsTimelineIpsRequest(
        string $projectId,
        string $environmentId,
        int $from,
        int $to,
        ?int $limit = null,
        ?int $topHitsCount = null,
        ?array $applications = null,
        ?string $applicationsMode = null,
        ?array $methods = null,
        ?string $methodsMode = null,
        ?array $domains = null,
        ?string $domainsMode = null,
        ?array $codeSlots = null,
        ?string $codeSlotsMode = null,
        ?array $codes = null,
        ?string $codesMode = null,
        ?array $requestDurationSlots = null,
        ?string $requestDurationSlotsMode = null
    ): RequestInterface {
        // verify the required parameter 'projectId' is set
        if (empty($projectId)) {
            throw new InvalidArgumentException(
                'Missing the required parameter $projectId
                when calling httpMetricsTimelineIps'
            );
        }

        if (!preg_match("/[a-z0-9]+/", $projectId)) {
            throw new InvalidArgumentException(
                "invalid value for \"projectId\" when calling HttpTrafficApi.httpMetricsTimelineIps,
                must conform to the pattern /[a-z0-9]+/."
            );
        }

        // verify the required parameter 'environmentId' is set
        if (empty($environmentId)) {
            throw new InvalidArgumentException(
                'Missing the required parameter $environmentId
                when calling httpMetricsTimelineIps'
            );
        }

        if (!preg_match("/.+/", $environmentId)) {
            throw new InvalidArgumentException(
                "invalid value for \"environmentId\" when calling HttpTrafficApi.httpMetricsTimelineIps,
                must conform to the pattern /.+/."
            );
        }

        // verify the required parameter 'from' is set
        if (empty($from)) {
            throw new InvalidArgumentException(
                'Missing the required parameter $from
                when calling httpMetricsTimelineIps'
            );
        }
        // verify the required parameter 'to' is set
        if (empty($to)) {
            throw new InvalidArgumentException(
                'Missing the required parameter $to
                when calling httpMetricsTimelineIps'
            );
        }

        if ($limit !== null && $limit > 300) {
            throw new InvalidArgumentException(
                'invalid value for "$limit" when calling HttpTrafficApi.httpMetricsTimelineIps,
                must be smaller than or equal to 300.'
            );
        }

        if ($limit !== null && $limit < 1) {
            throw new InvalidArgumentException(
                'invalid value for "$limit" when calling HttpTrafficApi.httpMetricsTimelineIps,
                must be bigger than or equal to 1.'
            );
        }

        if ($topHitsCount !== null && $topHitsCount > 15) {
            throw new InvalidArgumentException(
                'invalid value for "$topHitsCount" when calling HttpTrafficApi.httpMetricsTimelineIps,
                must be smaller than or equal to 15.'
            );
        }

        if ($topHitsCount !== null && $topHitsCount < 1) {
            throw new InvalidArgumentException(
                'invalid value for "$topHitsCount" when calling HttpTrafficApi.httpMetricsTimelineIps,
                must be bigger than or equal to 1.'
            );
        }

        $resourcePath = '/projects/{projectId}/environments/{environmentId}/observability/http/breakdown/ips';
        $formParams = [];
        $queryParams = [];
        $headerParams = [];
        $httpBody = null;
        $multipart = false;

        // query params
        if ($from !== null) {
            if ('form' === 'form' && is_array($from)) {
                foreach ($from as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['from'] = $from instanceof DateTime
                    ? $from->format(DATE_ATOM)
                    : ($from);
            }
        }

        // query params
        if ($to !== null) {
            if ('form' === 'form' && is_array($to)) {
                foreach ($to as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['to'] = $to instanceof DateTime
                    ? $to->format(DATE_ATOM)
                    : ($to);
            }
        }

        // query params
        if ($limit !== null) {
            if ('form' === 'form' && is_array($limit)) {
                foreach ($limit as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['limit'] = $limit instanceof DateTime
                    ? $limit->format(DATE_ATOM)
                    : ($limit);
            }
        }

        // query params
        if ($topHitsCount !== null) {
            if ('form' === 'form' && is_array($topHitsCount)) {
                foreach ($topHitsCount as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['top_hits_count'] = $topHitsCount instanceof DateTime
                    ? $topHitsCount->format(DATE_ATOM)
                    : ($topHitsCount);
            }
        }

        // query params
        if ($applications !== null) {
            if ('form' === 'form' && is_array($applications)) {
                foreach ($applications as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['applications[]'] = $applications instanceof DateTime
                    ? $applications->format(DATE_ATOM)
                    : ($applications);
            }
        }

        // query params
        if ($applicationsMode !== null) {
            if ('form' === 'form' && is_array($applicationsMode)) {
                foreach ($applicationsMode as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['applications_mode'] = $applicationsMode instanceof DateTime
                    ? $applicationsMode->format(DATE_ATOM)
                    : ($applicationsMode);
            }
        }

        // query params
        if ($methods !== null) {
            if ('form' === 'form' && is_array($methods)) {
                foreach ($methods as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['methods[]'] = $methods instanceof DateTime
                    ? $methods->format(DATE_ATOM)
                    : ($methods);
            }
        }

        // query params
        if ($methodsMode !== null) {
            if ('form' === 'form' && is_array($methodsMode)) {
                foreach ($methodsMode as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['methods_mode'] = $methodsMode instanceof DateTime
                    ? $methodsMode->format(DATE_ATOM)
                    : ($methodsMode);
            }
        }

        // query params
        if ($domains !== null) {
            if ('form' === 'form' && is_array($domains)) {
                foreach ($domains as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['domains[]'] = $domains instanceof DateTime
                    ? $domains->format(DATE_ATOM)
                    : ($domains);
            }
        }

        // query params
        if ($domainsMode !== null) {
            if ('form' === 'form' && is_array($domainsMode)) {
                foreach ($domainsMode as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['domains_mode'] = $domainsMode instanceof DateTime
                    ? $domainsMode->format(DATE_ATOM)
                    : ($domainsMode);
            }
        }

        // query params
        if ($codeSlots !== null) {
            if ('form' === 'form' && is_array($codeSlots)) {
                foreach ($codeSlots as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['code_slots[]'] = $codeSlots instanceof DateTime
                    ? $codeSlots->format(DATE_ATOM)
                    : ($codeSlots);
            }
        }

        // query params
        if ($codeSlotsMode !== null) {
            if ('form' === 'form' && is_array($codeSlotsMode)) {
                foreach ($codeSlotsMode as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['code_slots_mode'] = $codeSlotsMode instanceof DateTime
                    ? $codeSlotsMode->format(DATE_ATOM)
                    : ($codeSlotsMode);
            }
        }

        // query params
        if ($codes !== null) {
            if ('form' === 'form' && is_array($codes)) {
                foreach ($codes as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['codes[]'] = $codes instanceof DateTime
                    ? $codes->format(DATE_ATOM)
                    : ($codes);
            }
        }

        // query params
        if ($codesMode !== null) {
            if ('form' === 'form' && is_array($codesMode)) {
                foreach ($codesMode as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['codes_mode'] = $codesMode instanceof DateTime
                    ? $codesMode->format(DATE_ATOM)
                    : ($codesMode);
            }
        }

        // query params
        if ($requestDurationSlots !== null) {
            if ('form' === 'form' && is_array($requestDurationSlots)) {
                foreach ($requestDurationSlots as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['request_duration_slots[]'] = $requestDurationSlots instanceof DateTime
                    ? $requestDurationSlots->format(DATE_ATOM)
                    : ($requestDurationSlots);
            }
        }

        // query params
        if ($requestDurationSlotsMode !== null) {
            if ('form' === 'form' && is_array($requestDurationSlotsMode)) {
                foreach ($requestDurationSlotsMode as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['request_duration_slots_mode'] = $requestDurationSlotsMode instanceof DateTime
                    ? $requestDurationSlotsMode->format(DATE_ATOM)
                    : ($requestDurationSlotsMode);
            }
        }

        // path params

        if ($projectId !== null) {
            $resourcePath = str_replace(
                '{' . 'projectId' . '}',
                ObjectSerializer::toPathValue($projectId),
                $resourcePath
            );
        }
        // path params

        if ($environmentId !== null) {
            $resourcePath = str_replace(
                '{' . 'environmentId' . '}',
                ObjectSerializer::toPathValue($environmentId),
                $resourcePath
            );
        }

        $headers = $this->headerSelector->selectHeaders(
            ['application/json'],
            '',
            $multipart
        );

        // for model (json/xml)
        if ($formParams !== []) {
            if ($multipart) {
                $multipartContents = [];
                foreach ($formParams as $formParamName => $formParamValue) {
                    $formParamValueItems = is_array($formParamValue) ? $formParamValue : [$formParamValue];
                    foreach ($formParamValueItems as $formParamValueItem) {
                        $multipartContents[] = [
                            'name' => $formParamName,
                            'contents' => $formParamValueItem
                        ];
                    }
                }

                // for HTTP post (form)
                $httpBody = new MultipartStream($multipartContents);
            } elseif ($this->headerSelector->isJsonMime($headers['Content-Type'])) {
                $httpBody = json_encode($formParams);
            } else {
                // for HTTP post (form)
                $httpBody = ObjectSerializer::buildQuery($formParams);
            }
        }

        // this endpoint requires Bearer authentication (access token)
        if ($this->config->getAccessToken() !== null) {
            $headers['Authorization'] = 'Bearer ' . $this->config->getAccessToken();
        }

        $defaultHeaders = [];
        if ($this->config->getUserAgent()) {
            $defaultHeaders['User-Agent'] = $this->config->getUserAgent();
        }
        if ($this->config->getUpsunClientHeader()) {
            $defaultHeaders['X-Upsun-Client'] = $this->config->getUpsunClientHeader();
        }

        $headers = array_merge(
            $defaultHeaders,
            $headerParams,
            $headers
        );

        $operationHost = $this->config->getHost();

        $uri = $this->createUri($operationHost, $resourcePath, $queryParams);

        return $this->createRequest('GET', $uri, $headers, $httpBody);
    }

    /**
     * Get HTTP traffic timeline by URL
     *
     * Returns HTTP traffic analytics with URLs ranked by impact (response time × request count), aggregated metrics
     * (average, median, p96 response times in milliseconds), status code distributions, and time-series data. Use limit
     * parameter to control total items returned (default: 200, max: 300) and top_hits_count to control how many items
     * are flagged as top hits and tracked in the timeline (default: 10, max: 15). Items with index < top_hits_count are
     * flagged with top_hit: true and tracked in top_hits_timeline; the remainder (up to limit) are returned with
     * top_hit: false.
     *
     * @param  string $projectId (required)
     * @param  string $environmentId (required)
     * @param  int $from (required)
     * @param  int $to (required)
     * @param  int|null $limit (optional)
     * @param  int|null $topHitsCount (optional)
     * @param  string[]|null $applications (optional)
     * @param  string|null $applicationsMode (optional)
     * @param  string[]|null $methods (optional)
     * @param  string|null $methodsMode (optional)
     * @param  string[]|null $domains (optional)
     * @param  string|null $domainsMode (optional)
     * @param  string[]|null $codeSlots (optional)
     * @param  string|null $codeSlotsMode (optional)
     * @param  string[]|null $codes (optional)
     * @param  string|null $codesMode (optional)
     * @param  string[]|null $requestDurationSlots (optional)
     * @param  string|null $requestDurationSlotsMode (optional)
     *
     * @throws ApiException on non-2xx response or if the response body is not in the expected format
     * @throws ClientExceptionInterface
     * @see https://docs.upsun.com/api/#tag/Http-Traffic/operation/http_metrics_timeline_urls
     */
    public function httpMetricsTimelineUrls(
        string $projectId,
        string $environmentId,
        int $from,
        int $to,
        ?int $limit = null,
        ?int $topHitsCount = null,
        ?array $applications = null,
        ?string $applicationsMode = null,
        ?array $methods = null,
        ?string $methodsMode = null,
        ?array $domains = null,
        ?string $domainsMode = null,
        ?array $codeSlots = null,
        ?string $codeSlotsMode = null,
        ?array $codes = null,
        ?string $codesMode = null,
        ?array $requestDurationSlots = null,
        ?string $requestDurationSlotsMode = null
    ): mixed {
        return $this->httpMetricsTimelineUrlsWithHttpInfo(
            $projectId,
            $environmentId,
            $from,
            $to,
            $limit,
            $topHitsCount,
            $applications,
            $applicationsMode,
            $methods,
            $methodsMode,
            $domains,
            $domainsMode,
            $codeSlots,
            $codeSlotsMode,
            $codes,
            $codesMode,
            $requestDurationSlots,
            $requestDurationSlotsMode
        );
    }

    /**
     * Get HTTP traffic timeline by URL with HTTP Info
     *
     * @param  string $projectId (required)
     * @param  string $environmentId (required)
     * @param  int $from (required)
     * @param  int $to (required)
     * @param  int|null $limit (optional)
     * @param  int|null $topHitsCount (optional)
     * @param  string[]|null $applications (optional)
     * @param  string|null $applicationsMode (optional)
     * @param  string[]|null $methods (optional)
     * @param  string|null $methodsMode (optional)
     * @param  string[]|null $domains (optional)
     * @param  string|null $domainsMode (optional)
     * @param  string[]|null $codeSlots (optional)
     * @param  string|null $codeSlotsMode (optional)
     * @param  string[]|null $codes (optional)
     * @param  string|null $codesMode (optional)
     * @param  string[]|null $requestDurationSlots (optional)
     * @param  string|null $requestDurationSlotsMode (optional)
     *
     * @throws ApiException on non-2xx response or if the response body is not in the expected format
     * @throws ClientExceptionInterface
    */
    private function httpMetricsTimelineUrlsWithHttpInfo(
        string $projectId,
        string $environmentId,
        int $from,
        int $to,
        ?int $limit = null,
        ?int $topHitsCount = null,
        ?array $applications = null,
        ?string $applicationsMode = null,
        ?array $methods = null,
        ?string $methodsMode = null,
        ?array $domains = null,
        ?string $domainsMode = null,
        ?array $codeSlots = null,
        ?string $codeSlotsMode = null,
        ?array $codes = null,
        ?string $codesMode = null,
        ?array $requestDurationSlots = null,
        ?string $requestDurationSlotsMode = null
    ): mixed {
        $request = $this->httpMetricsTimelineUrlsRequest(
            $projectId,
            $environmentId,
            $from,
            $to,
            $limit,
            $topHitsCount,
            $applications,
            $applicationsMode,
            $methods,
            $methodsMode,
            $domains,
            $domainsMode,
            $codeSlots,
            $codeSlotsMode,
            $codes,
            $codesMode,
            $requestDurationSlots,
            $requestDurationSlotsMode
        );

        try {
            $response = $this->sendAuthenticatedRequest(
                $request->getMethod(),
                (string) $request->getUri(),
                $request->getHeaders(),
                $request->getBody()
            );

            return $this->handleResponseWithDataType(
                '\Upsun\Model\HttpMetricsTimelineUrls200Response',
                $request,
                $response
            );
        } catch (Exception $exception) {
            throw new ApiException(
                sprintf(
                    '[%d] Error connecting to the API (%s)',
                    $exception->getCode(),
                    '/projects/{projectId}/environments/{environmentId}/observability/http/breakdown/urls'
                ),
                $request,
                $response ?? null,
                $exception
            );
        }
    }

    /**
     * Create request for operation 'httpMetricsTimelineUrls'
     *
     * @param  string $projectId (required)
     * @param  string $environmentId (required)
     * @param  int $from (required)
     * @param  int $to (required)
     * @param  int|null $limit (optional)
     * @param  int|null $topHitsCount (optional)
     * @param  string[]|null $applications (optional)
     * @param  string|null $applicationsMode (optional)
     * @param  string[]|null $methods (optional)
     * @param  string|null $methodsMode (optional)
     * @param  string[]|null $domains (optional)
     * @param  string|null $domainsMode (optional)
     * @param  string[]|null $codeSlots (optional)
     * @param  string|null $codeSlotsMode (optional)
     * @param  string[]|null $codes (optional)
     * @param  string|null $codesMode (optional)
     * @param  string[]|null $requestDurationSlots (optional)
     * @param  string|null $requestDurationSlotsMode (optional)
     *
     * @throws InvalidArgumentException
     */
    private function httpMetricsTimelineUrlsRequest(
        string $projectId,
        string $environmentId,
        int $from,
        int $to,
        ?int $limit = null,
        ?int $topHitsCount = null,
        ?array $applications = null,
        ?string $applicationsMode = null,
        ?array $methods = null,
        ?string $methodsMode = null,
        ?array $domains = null,
        ?string $domainsMode = null,
        ?array $codeSlots = null,
        ?string $codeSlotsMode = null,
        ?array $codes = null,
        ?string $codesMode = null,
        ?array $requestDurationSlots = null,
        ?string $requestDurationSlotsMode = null
    ): RequestInterface {
        // verify the required parameter 'projectId' is set
        if (empty($projectId)) {
            throw new InvalidArgumentException(
                'Missing the required parameter $projectId
                when calling httpMetricsTimelineUrls'
            );
        }

        if (!preg_match("/[a-z0-9]+/", $projectId)) {
            throw new InvalidArgumentException(
                "invalid value for \"projectId\" when calling HttpTrafficApi.httpMetricsTimelineUrls,
                must conform to the pattern /[a-z0-9]+/."
            );
        }

        // verify the required parameter 'environmentId' is set
        if (empty($environmentId)) {
            throw new InvalidArgumentException(
                'Missing the required parameter $environmentId
                when calling httpMetricsTimelineUrls'
            );
        }

        if (!preg_match("/.+/", $environmentId)) {
            throw new InvalidArgumentException(
                "invalid value for \"environmentId\" when calling HttpTrafficApi.httpMetricsTimelineUrls,
                must conform to the pattern /.+/."
            );
        }

        // verify the required parameter 'from' is set
        if (empty($from)) {
            throw new InvalidArgumentException(
                'Missing the required parameter $from
                when calling httpMetricsTimelineUrls'
            );
        }
        // verify the required parameter 'to' is set
        if (empty($to)) {
            throw new InvalidArgumentException(
                'Missing the required parameter $to
                when calling httpMetricsTimelineUrls'
            );
        }

        if ($limit !== null && $limit > 300) {
            throw new InvalidArgumentException(
                'invalid value for "$limit" when calling HttpTrafficApi.httpMetricsTimelineUrls,
                must be smaller than or equal to 300.'
            );
        }

        if ($limit !== null && $limit < 1) {
            throw new InvalidArgumentException(
                'invalid value for "$limit" when calling HttpTrafficApi.httpMetricsTimelineUrls,
                must be bigger than or equal to 1.'
            );
        }

        if ($topHitsCount !== null && $topHitsCount > 15) {
            throw new InvalidArgumentException(
                'invalid value for "$topHitsCount" when calling HttpTrafficApi.httpMetricsTimelineUrls,
                must be smaller than or equal to 15.'
            );
        }

        if ($topHitsCount !== null && $topHitsCount < 1) {
            throw new InvalidArgumentException(
                'invalid value for "$topHitsCount" when calling HttpTrafficApi.httpMetricsTimelineUrls,
                must be bigger than or equal to 1.'
            );
        }

        $resourcePath = '/projects/{projectId}/environments/{environmentId}/observability/http/breakdown/urls';
        $formParams = [];
        $queryParams = [];
        $headerParams = [];
        $httpBody = null;
        $multipart = false;

        // query params
        if ($from !== null) {
            if ('form' === 'form' && is_array($from)) {
                foreach ($from as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['from'] = $from instanceof DateTime
                    ? $from->format(DATE_ATOM)
                    : ($from);
            }
        }

        // query params
        if ($to !== null) {
            if ('form' === 'form' && is_array($to)) {
                foreach ($to as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['to'] = $to instanceof DateTime
                    ? $to->format(DATE_ATOM)
                    : ($to);
            }
        }

        // query params
        if ($limit !== null) {
            if ('form' === 'form' && is_array($limit)) {
                foreach ($limit as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['limit'] = $limit instanceof DateTime
                    ? $limit->format(DATE_ATOM)
                    : ($limit);
            }
        }

        // query params
        if ($topHitsCount !== null) {
            if ('form' === 'form' && is_array($topHitsCount)) {
                foreach ($topHitsCount as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['top_hits_count'] = $topHitsCount instanceof DateTime
                    ? $topHitsCount->format(DATE_ATOM)
                    : ($topHitsCount);
            }
        }

        // query params
        if ($applications !== null) {
            if ('form' === 'form' && is_array($applications)) {
                foreach ($applications as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['applications[]'] = $applications instanceof DateTime
                    ? $applications->format(DATE_ATOM)
                    : ($applications);
            }
        }

        // query params
        if ($applicationsMode !== null) {
            if ('form' === 'form' && is_array($applicationsMode)) {
                foreach ($applicationsMode as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['applications_mode'] = $applicationsMode instanceof DateTime
                    ? $applicationsMode->format(DATE_ATOM)
                    : ($applicationsMode);
            }
        }

        // query params
        if ($methods !== null) {
            if ('form' === 'form' && is_array($methods)) {
                foreach ($methods as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['methods[]'] = $methods instanceof DateTime
                    ? $methods->format(DATE_ATOM)
                    : ($methods);
            }
        }

        // query params
        if ($methodsMode !== null) {
            if ('form' === 'form' && is_array($methodsMode)) {
                foreach ($methodsMode as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['methods_mode'] = $methodsMode instanceof DateTime
                    ? $methodsMode->format(DATE_ATOM)
                    : ($methodsMode);
            }
        }

        // query params
        if ($domains !== null) {
            if ('form' === 'form' && is_array($domains)) {
                foreach ($domains as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['domains[]'] = $domains instanceof DateTime
                    ? $domains->format(DATE_ATOM)
                    : ($domains);
            }
        }

        // query params
        if ($domainsMode !== null) {
            if ('form' === 'form' && is_array($domainsMode)) {
                foreach ($domainsMode as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['domains_mode'] = $domainsMode instanceof DateTime
                    ? $domainsMode->format(DATE_ATOM)
                    : ($domainsMode);
            }
        }

        // query params
        if ($codeSlots !== null) {
            if ('form' === 'form' && is_array($codeSlots)) {
                foreach ($codeSlots as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['code_slots[]'] = $codeSlots instanceof DateTime
                    ? $codeSlots->format(DATE_ATOM)
                    : ($codeSlots);
            }
        }

        // query params
        if ($codeSlotsMode !== null) {
            if ('form' === 'form' && is_array($codeSlotsMode)) {
                foreach ($codeSlotsMode as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['code_slots_mode'] = $codeSlotsMode instanceof DateTime
                    ? $codeSlotsMode->format(DATE_ATOM)
                    : ($codeSlotsMode);
            }
        }

        // query params
        if ($codes !== null) {
            if ('form' === 'form' && is_array($codes)) {
                foreach ($codes as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['codes[]'] = $codes instanceof DateTime
                    ? $codes->format(DATE_ATOM)
                    : ($codes);
            }
        }

        // query params
        if ($codesMode !== null) {
            if ('form' === 'form' && is_array($codesMode)) {
                foreach ($codesMode as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['codes_mode'] = $codesMode instanceof DateTime
                    ? $codesMode->format(DATE_ATOM)
                    : ($codesMode);
            }
        }

        // query params
        if ($requestDurationSlots !== null) {
            if ('form' === 'form' && is_array($requestDurationSlots)) {
                foreach ($requestDurationSlots as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['request_duration_slots[]'] = $requestDurationSlots instanceof DateTime
                    ? $requestDurationSlots->format(DATE_ATOM)
                    : ($requestDurationSlots);
            }
        }

        // query params
        if ($requestDurationSlotsMode !== null) {
            if ('form' === 'form' && is_array($requestDurationSlotsMode)) {
                foreach ($requestDurationSlotsMode as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['request_duration_slots_mode'] = $requestDurationSlotsMode instanceof DateTime
                    ? $requestDurationSlotsMode->format(DATE_ATOM)
                    : ($requestDurationSlotsMode);
            }
        }

        // path params

        if ($projectId !== null) {
            $resourcePath = str_replace(
                '{' . 'projectId' . '}',
                ObjectSerializer::toPathValue($projectId),
                $resourcePath
            );
        }
        // path params

        if ($environmentId !== null) {
            $resourcePath = str_replace(
                '{' . 'environmentId' . '}',
                ObjectSerializer::toPathValue($environmentId),
                $resourcePath
            );
        }

        $headers = $this->headerSelector->selectHeaders(
            ['application/json'],
            '',
            $multipart
        );

        // for model (json/xml)
        if ($formParams !== []) {
            if ($multipart) {
                $multipartContents = [];
                foreach ($formParams as $formParamName => $formParamValue) {
                    $formParamValueItems = is_array($formParamValue) ? $formParamValue : [$formParamValue];
                    foreach ($formParamValueItems as $formParamValueItem) {
                        $multipartContents[] = [
                            'name' => $formParamName,
                            'contents' => $formParamValueItem
                        ];
                    }
                }

                // for HTTP post (form)
                $httpBody = new MultipartStream($multipartContents);
            } elseif ($this->headerSelector->isJsonMime($headers['Content-Type'])) {
                $httpBody = json_encode($formParams);
            } else {
                // for HTTP post (form)
                $httpBody = ObjectSerializer::buildQuery($formParams);
            }
        }

        // this endpoint requires Bearer authentication (access token)
        if ($this->config->getAccessToken() !== null) {
            $headers['Authorization'] = 'Bearer ' . $this->config->getAccessToken();
        }

        $defaultHeaders = [];
        if ($this->config->getUserAgent()) {
            $defaultHeaders['User-Agent'] = $this->config->getUserAgent();
        }
        if ($this->config->getUpsunClientHeader()) {
            $defaultHeaders['X-Upsun-Client'] = $this->config->getUpsunClientHeader();
        }

        $headers = array_merge(
            $defaultHeaders,
            $headerParams,
            $headers
        );

        $operationHost = $this->config->getHost();

        $uri = $this->createUri($operationHost, $resourcePath, $queryParams);

        return $this->createRequest('GET', $uri, $headers, $httpBody);
    }

    /**
     * Get HTTP traffic timeline by user agent
     *
     * Returns HTTP traffic analytics grouped by User Agent with User Agents ranked by impact (response time × request
     * count). Use limit parameter to control total items returned (default: 200, max: 300) and top_hits_count to
     * control how many items are flagged as top hits and tracked in the timeline (default: 10, max: 15). Items with
     * index < top_hits_count are flagged with top_hit: true and tracked in top_hits_timeline; the remainder (up to
     * limit) are returned with top_hit: false.
     *
     * @param  string $projectId (required)
     * @param  string $environmentId (required)
     * @param  int $from (required)
     * @param  int $to (required)
     * @param  int|null $limit (optional)
     * @param  int|null $topHitsCount (optional)
     * @param  string[]|null $applications (optional)
     * @param  string|null $applicationsMode (optional)
     * @param  string[]|null $methods (optional)
     * @param  string|null $methodsMode (optional)
     * @param  string[]|null $domains (optional)
     * @param  string|null $domainsMode (optional)
     * @param  string[]|null $codeSlots (optional)
     * @param  string|null $codeSlotsMode (optional)
     * @param  string[]|null $codes (optional)
     * @param  string|null $codesMode (optional)
     * @param  string[]|null $requestDurationSlots (optional)
     * @param  string|null $requestDurationSlotsMode (optional)
     *
     * @throws ApiException on non-2xx response or if the response body is not in the expected format
     * @throws ClientExceptionInterface
     * @see https://docs.upsun.com/api/#tag/Http-Traffic/operation/http_metrics_timeline_user_agents
     */
    public function httpMetricsTimelineUserAgents(
        string $projectId,
        string $environmentId,
        int $from,
        int $to,
        ?int $limit = null,
        ?int $topHitsCount = null,
        ?array $applications = null,
        ?string $applicationsMode = null,
        ?array $methods = null,
        ?string $methodsMode = null,
        ?array $domains = null,
        ?string $domainsMode = null,
        ?array $codeSlots = null,
        ?string $codeSlotsMode = null,
        ?array $codes = null,
        ?string $codesMode = null,
        ?array $requestDurationSlots = null,
        ?string $requestDurationSlotsMode = null
    ): mixed {
        return $this->httpMetricsTimelineUserAgentsWithHttpInfo(
            $projectId,
            $environmentId,
            $from,
            $to,
            $limit,
            $topHitsCount,
            $applications,
            $applicationsMode,
            $methods,
            $methodsMode,
            $domains,
            $domainsMode,
            $codeSlots,
            $codeSlotsMode,
            $codes,
            $codesMode,
            $requestDurationSlots,
            $requestDurationSlotsMode
        );
    }

    /**
     * Get HTTP traffic timeline by user agent with HTTP Info
     *
     * @param  string $projectId (required)
     * @param  string $environmentId (required)
     * @param  int $from (required)
     * @param  int $to (required)
     * @param  int|null $limit (optional)
     * @param  int|null $topHitsCount (optional)
     * @param  string[]|null $applications (optional)
     * @param  string|null $applicationsMode (optional)
     * @param  string[]|null $methods (optional)
     * @param  string|null $methodsMode (optional)
     * @param  string[]|null $domains (optional)
     * @param  string|null $domainsMode (optional)
     * @param  string[]|null $codeSlots (optional)
     * @param  string|null $codeSlotsMode (optional)
     * @param  string[]|null $codes (optional)
     * @param  string|null $codesMode (optional)
     * @param  string[]|null $requestDurationSlots (optional)
     * @param  string|null $requestDurationSlotsMode (optional)
     *
     * @throws ApiException on non-2xx response or if the response body is not in the expected format
     * @throws ClientExceptionInterface
    */
    private function httpMetricsTimelineUserAgentsWithHttpInfo(
        string $projectId,
        string $environmentId,
        int $from,
        int $to,
        ?int $limit = null,
        ?int $topHitsCount = null,
        ?array $applications = null,
        ?string $applicationsMode = null,
        ?array $methods = null,
        ?string $methodsMode = null,
        ?array $domains = null,
        ?string $domainsMode = null,
        ?array $codeSlots = null,
        ?string $codeSlotsMode = null,
        ?array $codes = null,
        ?string $codesMode = null,
        ?array $requestDurationSlots = null,
        ?string $requestDurationSlotsMode = null
    ): mixed {
        $request = $this->httpMetricsTimelineUserAgentsRequest(
            $projectId,
            $environmentId,
            $from,
            $to,
            $limit,
            $topHitsCount,
            $applications,
            $applicationsMode,
            $methods,
            $methodsMode,
            $domains,
            $domainsMode,
            $codeSlots,
            $codeSlotsMode,
            $codes,
            $codesMode,
            $requestDurationSlots,
            $requestDurationSlotsMode
        );

        try {
            $response = $this->sendAuthenticatedRequest(
                $request->getMethod(),
                (string) $request->getUri(),
                $request->getHeaders(),
                $request->getBody()
            );

            return $this->handleResponseWithDataType(
                '\Upsun\Model\HttpMetricsTimelineUserAgents200Response',
                $request,
                $response
            );
        } catch (Exception $exception) {
            throw new ApiException(
                sprintf(
                    '[%d] Error connecting to the API (%s)',
                    $exception->getCode(),
                    '/projects/{projectId}/environments/{environmentId}/observability/http/breakdown/user-agents'
                ),
                $request,
                $response ?? null,
                $exception
            );
        }
    }

    /**
     * Create request for operation 'httpMetricsTimelineUserAgents'
     *
     * @param  string $projectId (required)
     * @param  string $environmentId (required)
     * @param  int $from (required)
     * @param  int $to (required)
     * @param  int|null $limit (optional)
     * @param  int|null $topHitsCount (optional)
     * @param  string[]|null $applications (optional)
     * @param  string|null $applicationsMode (optional)
     * @param  string[]|null $methods (optional)
     * @param  string|null $methodsMode (optional)
     * @param  string[]|null $domains (optional)
     * @param  string|null $domainsMode (optional)
     * @param  string[]|null $codeSlots (optional)
     * @param  string|null $codeSlotsMode (optional)
     * @param  string[]|null $codes (optional)
     * @param  string|null $codesMode (optional)
     * @param  string[]|null $requestDurationSlots (optional)
     * @param  string|null $requestDurationSlotsMode (optional)
     *
     * @throws InvalidArgumentException
     */
    private function httpMetricsTimelineUserAgentsRequest(
        string $projectId,
        string $environmentId,
        int $from,
        int $to,
        ?int $limit = null,
        ?int $topHitsCount = null,
        ?array $applications = null,
        ?string $applicationsMode = null,
        ?array $methods = null,
        ?string $methodsMode = null,
        ?array $domains = null,
        ?string $domainsMode = null,
        ?array $codeSlots = null,
        ?string $codeSlotsMode = null,
        ?array $codes = null,
        ?string $codesMode = null,
        ?array $requestDurationSlots = null,
        ?string $requestDurationSlotsMode = null
    ): RequestInterface {
        // verify the required parameter 'projectId' is set
        if (empty($projectId)) {
            throw new InvalidArgumentException(
                'Missing the required parameter $projectId
                when calling httpMetricsTimelineUserAgents'
            );
        }

        if (!preg_match("/[a-z0-9]+/", $projectId)) {
            throw new InvalidArgumentException(
                "invalid value for \"projectId\" when calling HttpTrafficApi.httpMetricsTimelineUserAgents,
                must conform to the pattern /[a-z0-9]+/."
            );
        }

        // verify the required parameter 'environmentId' is set
        if (empty($environmentId)) {
            throw new InvalidArgumentException(
                'Missing the required parameter $environmentId
                when calling httpMetricsTimelineUserAgents'
            );
        }

        if (!preg_match("/.+/", $environmentId)) {
            throw new InvalidArgumentException(
                "invalid value for \"environmentId\" when calling HttpTrafficApi.httpMetricsTimelineUserAgents,
                must conform to the pattern /.+/."
            );
        }

        // verify the required parameter 'from' is set
        if (empty($from)) {
            throw new InvalidArgumentException(
                'Missing the required parameter $from
                when calling httpMetricsTimelineUserAgents'
            );
        }
        // verify the required parameter 'to' is set
        if (empty($to)) {
            throw new InvalidArgumentException(
                'Missing the required parameter $to
                when calling httpMetricsTimelineUserAgents'
            );
        }

        if ($limit !== null && $limit > 300) {
            throw new InvalidArgumentException(
                'invalid value for "$limit" when calling HttpTrafficApi.httpMetricsTimelineUserAgents,
                must be smaller than or equal to 300.'
            );
        }

        if ($limit !== null && $limit < 1) {
            throw new InvalidArgumentException(
                'invalid value for "$limit" when calling HttpTrafficApi.httpMetricsTimelineUserAgents,
                must be bigger than or equal to 1.'
            );
        }

        if ($topHitsCount !== null && $topHitsCount > 15) {
            throw new InvalidArgumentException(
                'invalid value for "$topHitsCount" when calling HttpTrafficApi.httpMetricsTimelineUserAgents,
                must be smaller than or equal to 15.'
            );
        }

        if ($topHitsCount !== null && $topHitsCount < 1) {
            throw new InvalidArgumentException(
                'invalid value for "$topHitsCount" when calling HttpTrafficApi.httpMetricsTimelineUserAgents,
                must be bigger than or equal to 1.'
            );
        }

        $resourcePath = '/projects/{projectId}/environments/{environmentId}/observability/http/breakdown/user-agents';
        $formParams = [];
        $queryParams = [];
        $headerParams = [];
        $httpBody = null;
        $multipart = false;

        // query params
        if ($from !== null) {
            if ('form' === 'form' && is_array($from)) {
                foreach ($from as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['from'] = $from instanceof DateTime
                    ? $from->format(DATE_ATOM)
                    : ($from);
            }
        }

        // query params
        if ($to !== null) {
            if ('form' === 'form' && is_array($to)) {
                foreach ($to as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['to'] = $to instanceof DateTime
                    ? $to->format(DATE_ATOM)
                    : ($to);
            }
        }

        // query params
        if ($limit !== null) {
            if ('form' === 'form' && is_array($limit)) {
                foreach ($limit as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['limit'] = $limit instanceof DateTime
                    ? $limit->format(DATE_ATOM)
                    : ($limit);
            }
        }

        // query params
        if ($topHitsCount !== null) {
            if ('form' === 'form' && is_array($topHitsCount)) {
                foreach ($topHitsCount as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['top_hits_count'] = $topHitsCount instanceof DateTime
                    ? $topHitsCount->format(DATE_ATOM)
                    : ($topHitsCount);
            }
        }

        // query params
        if ($applications !== null) {
            if ('form' === 'form' && is_array($applications)) {
                foreach ($applications as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['applications[]'] = $applications instanceof DateTime
                    ? $applications->format(DATE_ATOM)
                    : ($applications);
            }
        }

        // query params
        if ($applicationsMode !== null) {
            if ('form' === 'form' && is_array($applicationsMode)) {
                foreach ($applicationsMode as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['applications_mode'] = $applicationsMode instanceof DateTime
                    ? $applicationsMode->format(DATE_ATOM)
                    : ($applicationsMode);
            }
        }

        // query params
        if ($methods !== null) {
            if ('form' === 'form' && is_array($methods)) {
                foreach ($methods as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['methods[]'] = $methods instanceof DateTime
                    ? $methods->format(DATE_ATOM)
                    : ($methods);
            }
        }

        // query params
        if ($methodsMode !== null) {
            if ('form' === 'form' && is_array($methodsMode)) {
                foreach ($methodsMode as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['methods_mode'] = $methodsMode instanceof DateTime
                    ? $methodsMode->format(DATE_ATOM)
                    : ($methodsMode);
            }
        }

        // query params
        if ($domains !== null) {
            if ('form' === 'form' && is_array($domains)) {
                foreach ($domains as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['domains[]'] = $domains instanceof DateTime
                    ? $domains->format(DATE_ATOM)
                    : ($domains);
            }
        }

        // query params
        if ($domainsMode !== null) {
            if ('form' === 'form' && is_array($domainsMode)) {
                foreach ($domainsMode as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['domains_mode'] = $domainsMode instanceof DateTime
                    ? $domainsMode->format(DATE_ATOM)
                    : ($domainsMode);
            }
        }

        // query params
        if ($codeSlots !== null) {
            if ('form' === 'form' && is_array($codeSlots)) {
                foreach ($codeSlots as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['code_slots[]'] = $codeSlots instanceof DateTime
                    ? $codeSlots->format(DATE_ATOM)
                    : ($codeSlots);
            }
        }

        // query params
        if ($codeSlotsMode !== null) {
            if ('form' === 'form' && is_array($codeSlotsMode)) {
                foreach ($codeSlotsMode as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['code_slots_mode'] = $codeSlotsMode instanceof DateTime
                    ? $codeSlotsMode->format(DATE_ATOM)
                    : ($codeSlotsMode);
            }
        }

        // query params
        if ($codes !== null) {
            if ('form' === 'form' && is_array($codes)) {
                foreach ($codes as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['codes[]'] = $codes instanceof DateTime
                    ? $codes->format(DATE_ATOM)
                    : ($codes);
            }
        }

        // query params
        if ($codesMode !== null) {
            if ('form' === 'form' && is_array($codesMode)) {
                foreach ($codesMode as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['codes_mode'] = $codesMode instanceof DateTime
                    ? $codesMode->format(DATE_ATOM)
                    : ($codesMode);
            }
        }

        // query params
        if ($requestDurationSlots !== null) {
            if ('form' === 'form' && is_array($requestDurationSlots)) {
                foreach ($requestDurationSlots as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['request_duration_slots[]'] = $requestDurationSlots instanceof DateTime
                    ? $requestDurationSlots->format(DATE_ATOM)
                    : ($requestDurationSlots);
            }
        }

        // query params
        if ($requestDurationSlotsMode !== null) {
            if ('form' === 'form' && is_array($requestDurationSlotsMode)) {
                foreach ($requestDurationSlotsMode as $key => $value) {
                    $queryParams[$key] = $value;
                }
            } else {
                $queryParams['request_duration_slots_mode'] = $requestDurationSlotsMode instanceof DateTime
                    ? $requestDurationSlotsMode->format(DATE_ATOM)
                    : ($requestDurationSlotsMode);
            }
        }

        // path params

        if ($projectId !== null) {
            $resourcePath = str_replace(
                '{' . 'projectId' . '}',
                ObjectSerializer::toPathValue($projectId),
                $resourcePath
            );
        }
        // path params

        if ($environmentId !== null) {
            $resourcePath = str_replace(
                '{' . 'environmentId' . '}',
                ObjectSerializer::toPathValue($environmentId),
                $resourcePath
            );
        }

        $headers = $this->headerSelector->selectHeaders(
            ['application/json'],
            '',
            $multipart
        );

        // for model (json/xml)
        if ($formParams !== []) {
            if ($multipart) {
                $multipartContents = [];
                foreach ($formParams as $formParamName => $formParamValue) {
                    $formParamValueItems = is_array($formParamValue) ? $formParamValue : [$formParamValue];
                    foreach ($formParamValueItems as $formParamValueItem) {
                        $multipartContents[] = [
                            'name' => $formParamName,
                            'contents' => $formParamValueItem
                        ];
                    }
                }

                // for HTTP post (form)
                $httpBody = new MultipartStream($multipartContents);
            } elseif ($this->headerSelector->isJsonMime($headers['Content-Type'])) {
                $httpBody = json_encode($formParams);
            } else {
                // for HTTP post (form)
                $httpBody = ObjectSerializer::buildQuery($formParams);
            }
        }

        // this endpoint requires Bearer authentication (access token)
        if ($this->config->getAccessToken() !== null) {
            $headers['Authorization'] = 'Bearer ' . $this->config->getAccessToken();
        }

        $defaultHeaders = [];
        if ($this->config->getUserAgent()) {
            $defaultHeaders['User-Agent'] = $this->config->getUserAgent();
        }
        if ($this->config->getUpsunClientHeader()) {
            $defaultHeaders['X-Upsun-Client'] = $this->config->getUpsunClientHeader();
        }

        $headers = array_merge(
            $defaultHeaders,
            $headerParams,
            $headers
        );

        $operationHost = $this->config->getHost();

        $uri = $this->createUri($operationHost, $resourcePath, $queryParams);

        return $this->createRequest('GET', $uri, $headers, $httpBody);
    }
}
